# 考研数学笔记站（math-note）开发全流程总结

> 项目：考研数学（数二）笔记阅读 PWA
> 本地路径：`D:\ai code\math-note`　|　线上：GitHub Pages（仓库 `Cyrusssxx/math_learn`，main 分支）
> 开发周期：2026-07 　|　最终版本：Service Worker 缓存 v19
> 配套产出：用户级 skill `note`（`C:\Users\cjx\.qoder\skills\note\SKILL.md`）、产品功能总结 `FEATURES.md`

---

## 一、项目缘起：从"想做思维导图"到"自建笔记站"

### 1.1 起点：现成软件选型失败

最初的需求只是**把「框图版（@南门朔制作）」的高数/线代知识点 PDF 做成思维导图**，先后尝试：

| 尝试 | 结果 |
|---|---|
| Obsidian + markmap（md 思维导图） | 能渲染，逐步调出放大、全屏、左右布局、展开级别 -1 全展开、补充小例子 |
| XMind | **不支持 LaTeX 公式渲染**，数学笔记基本不可用 |
| 其他纯导图软件调研 | 公式支持普遍差 |

用户结论："**完全不能忍受**" —— 数学笔记的核心是公式，现成导图工具全部出局。

### 1.2 转折一：自建"记忆卡片系统"

参考自己已有的 408 刷题小程序体验，决定**单独做一个本地的、知识点分小节、带记忆系统的卡片应用**。先上网调研了主流方案（Anki/FSRS 间隔重复算法等），产出：

- `tools/build_cards.py`（281 行，卡片打包）
- `tools/test_fsrs.js`（FSRS 算法验证）
- 计划文档《数学记忆卡片PWA》

### 1.3 转折二：砍掉卡片，定型"笔记阅读站"

试用后用户拍板：

> "简单的东西全部合并，复习卡片功能不是重点，**重点是章节导航**，把东西放一块直接看复习" → "**算了我不要卡片了**"

产品最终定位：**离线可用、公式完美渲染、可批注、可搜索的考研数学笔记阅读站**。FSRS/卡片代码保留在 tools 中不再使用。

---

## 二、技术选型与架构

### 2.1 技术栈（零框架、零构建工具、零后端）

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | 原生 HTML/CSS/JS（无框架） | 单页阅读器，体量小，长期免维护 |
| 公式 | KaTeX（**完全本地化** vendor/，含 20 个字体文件） | 离线可用、渲染快，`tools/fetch_katex.py` 一键抓取 |
| 离线 | Service Worker 全站预缓存（PWA，可安装） | 无网环境（自习室）照常使用 |
| 标注存储 | localStorage（文本批注/荧光）+ IndexedDB（贴图 blob） | localStorage 放不下图片 |
| 构建 | Python 脚本（`tools/build_notes.py`，`-X utf8`） | 把笔记 md 打包成单个 `notes.json` |
| 部署 | GitHub Pages | 免费、推送即上线 |

### 2.2 数据流

```
D:\ai code\math\导图\*.md（17 份） ┐
D:\ai code\math\好题\*.md（36 份） ┤←—— 唯一可信内容源（人工/AI 维护）
                                   ↓ python -X utf8 tools/build_notes.py
math-note\pwa\data\notes.json      ←—— 打包产物（勿手改；本地图片自动复制到 data/img/）
                                   ↓ 前端 fetch + 自定义渲染
index.html + reader.js（渲染/路由/搜索）+ annotate.js（批注）+ common.js
                                   ↓ sw.js 预缓存（CACHE_VER 版本控制）
本地 start.bat → localhost:8409　/　线上 GitHub Pages
```

### 2.3 自定义 md 语法子集（reader.js 渲染，非完整 Markdown）

- `# 页标题`、`## 章节`（生成右侧目录+锚点）、`- ` 嵌套列表（2 空格一级）、`| |` 表格
- `::: fold 标题 … :::` 折叠块（答案/解析默认收起）
- `$...$` 行内公式；表格分列/自动分行逻辑均忽略公式内的 `|` `；` `：`
- `![说明](路径)`：独行 → figure 大图+图注；行内 → 小图
- 条目标记自动着色：`⚠️` 易错、`💡` 技巧、`例：` 例题、`⭐` 常考（可叠加）
- 文件名规范：`高数N-xxx / 线代N-xxx / 好题N-xxx`（N 可带小数决定排序，build 强校验）

---

## 三、开发阶段全记录

### 阶段 1：基础站搭建（首版 PWA）

- 三栏布局：左侧科目目录树（高数/线代/好题，可折叠）、中间正文、右侧本页目录（滚动高亮）
- KaTeX 本地化渲染、hash 路由、夜间模式、阅读计时（切窗自动暂停）
- Service Worker 全站预缓存 + `manifest.webmanifest`，Chrome 可安装为桌面应用
- `start.bat` 一键启动（SW 必须走 http://，file:// 不可用）

### 阶段 2：内容建设（数据比代码难）

多轮资料整合，全部收敛到 `math\导图\` 与 `math\好题\`：

1. 框图版 PDF → 初版知识点 md
2. 按《线代大复盘》《高数2e》《27Kira 公式大全》整体补充合并
3. 用户指出内容太少太简洁 → 按《Kira 线代强化讲义》**前 93 页**（只取知识点公式，不取题目）完善线代，讲义中的填空处一并补全
4. **考纲裁剪**：数二不考概率论/三重积分/曲线曲面积分等，删除全部数一内容
5. 公式小错误修复（如特征向量表述）、重要/常考知识点前置、颜色加粗标注

### 阶段 3：阅读体验打磨（用户逐条交代的硬要求）

- **排版不密集**：li 间距 11px、行高 1.85、顶层条目 22px——"方便观看"是铁律
- **长条目自动分行**：`breakLines()` 在公式外 `；` 后断行、有分号时首个公式外 `：` 后断行（跳过 `$...$` 内部标点）
- 图片嵌入笔记（build 时自动搬运），后期加**双击全屏放大** lightbox（Esc/点遮罩关闭）

### 阶段 4：批注系统（annotate.js，411 行）

用户核心诉求："可否自己添加批注、荧光重点、笔记啥的，在网页上实现" → "我要"。

三种标注，统一由选中文字/点击块后的浮动工具条触发：

| 能力 | 实现要点 |
|---|---|
| 选中文字荧光（多色） | `mark.mk` 跨文本节点包裹（wrapText），**自动避开 KaTeX 公式内部**、支持跨加粗边界；可改色/取消 |
| 整块着色 | 整条 li/p 背景色 |
| 📝 文字批注 | 块下方批注框，可编辑；**删除必须 confirm，修改不弹窗**（用户明确交代） |

关键设计——**文本锚点**：锚 = `hash(ownText(block))`（块自身文本，不含子列表），存 `localStorage['notesAnnot']` 按笔记 id 分桶，渲染完（KaTeX 之后）`Annot.apply()` 恢复。
由此产生全项目第一铁律：**任何渲染改动都不得增删改块级元素的 textContent，只能插标签**——否则用户所有标注失效。

### 阶段 5：批注贴图（先评估后实施）

用户流程很典型："可以直接用粘贴板的图片复制进去吗？**评可行性，先别执行**" → 给出方案对比 → 用户选定"B1：存浏览器 IndexedDB + 纳入现有导出迁移机制"后才动手：

- 编辑框内 Ctrl+V 截获 paste 事件（image/*），图片 blob 存 IndexedDB（库 `notesAnnotImg`），正文用 `[图:id]` 占位
- 展示时占位符替换为 `<img class="ann-img">` 异步填 blob，可双击放大
- **三处 GC**（防 IndexedDB 泄漏）：保存时删失引用图、取消时删新贴未存图、删批注时删其全部图
- 导出升级 v2 格式 `{__fmt,__v:2,annot,imgs}`：引用图转 base64 随 JSON 打包；导入兼容旧格式（无 `__v` 视为纯 annot），换浏览器/电脑无缝迁移

### 阶段 6：搜索系统

- **索引**：笔记标题(w=0)/章节(w=1)/正文行(w=2)+📝用户批注（实时读 localStorage，排最前）
- **排序**（用户明确要求）：知识点优先（高数→线代），好题靠后；标题命中先于正文命中；结果带科目标签，取前 40 条
- **精确定位**（修 bug 时重做）：点结果不是跳章节，而是定位**命中那一行**——取命中文本最长的公式外片段做锚，在 li/p/h2/td/批注中找文本最短的命中块（最内层最精确），instant 滚到屏幕中央 + 黄色闪烁 1.8s，折叠块自动展开；跨笔记用 `pendingLocate` 渲染后消费
- **搜索历史**：最多 5 条、去重置顶、只在点击结果时记录（证明有效搜索）；聚焦空框弹出、单条×删、一键清空、点外部收起；上线后按用户要求二次缩短行距

### 阶段 7：关键 Bug——"搜索定位跳转到开头"

最有价值的一次排查。现象：点搜索结果回到页面顶部。用浏览器 MCP 逐步定位：

1. scrollHeight 正常 → 排除渲染问题
2. `window.scrollTo` 调用后 scrollY 恒 0 → 滚动本身没生效
3. 发现 `document.hidden === true`（自动化测试时页面在后台标签页）
4. **根因：`scroll-behavior: smooth` 的平滑滚动在后台标签页完全不执行**，且可被后续操作打断

修复：所有程序化滚动一律 `behavior:'instant'`，并顺势把"跳章节"升级成上面的"定位命中行+闪烁"。此坑写入 skill 成为第二铁律。

### 阶段 8：部署上线

1. 用户提供 GitHub 标准命令序列，确认目标是 math-note 项目（"别搞混了"）后执行
2. 保护性调整：跳过 `echo "# math_learn" >> README.md`（已有完整 README，追加会破坏）
3. 首推 40 文件 / 2150 行；push 遇 `Connection was reset` → 临时代理 `HTTPS_PROXY=http://127.0.0.1:7890`（iKuuuVPN）解决
4. 用户开启 GitHub Pages；此后约定：**改完默认直接推送上线，推送前无需询问、无需安全扫描**
5. 缓存更新机制：每次改 `pwa/` 下任何文件必须 bump `sw.js` 的 `CACHE_VER`（本项目走到 v19），客户端刷新两次即用新版

### 阶段 9：资产沉淀与收尾

- **skill 沉淀**：把全部工作流总结成 skill，经三轮补充（批注规范→搜索规范+用户体验原则→加"产品功能总结"环节），最终定型为**用户级**通用笔记 skill `note`（9+1 章节，125 行）
- **便签风格应用图标**：`tools/make_icon.py` 用 PIL 程序化绘制（主题蓝圆角底 + 便签黄纸 + 右下折角 + 三行笔记线，4 倍超采样），192/512 双尺寸，补 favicon 引用
- **FEATURES.md**：面向使用者的产品功能全量总结（阅读/搜索/标注/迁移/PWA 五模块），随代码维护

---

## 四、Git 提交史

| 提交 | 内容 |
|---|---|
| `84dfdff` first commit | 全站 40 文件 2150 行（此前开发均在 git 之前完成） |
| `c46779e` | 搜索历史(可删/清空) + 图片双击放大 + 搜索精确定位命中行 |
| `8661b87` | 搜索历史缩短：最多 5 条 + 紧凑行距 |
| `e7438f0` | 便签风格应用图标 + favicon + FEATURES.md |

---

## 五、沉淀下来的铁律与经验

### 5.1 产品/代码铁律

1. **块级 textContent 不可变**：批注锚点基于文本 hash，渲染改动只能插标签
2. **程序化滚动一律 instant**：smooth 在后台标签页不执行
3. **改 pwa/ 必 bump CACHE_VER**：否则用户永远用旧缓存
4. **贴图三处 GC 缺一不可**：否则 IndexedDB 泄漏
5. **批注新能力必须同步接入导出/导入**：保证可迁移
6. **敏感操作才 confirm**：删除要确认，修改不弹窗
7. **排版宁疏勿密、长条目自动分行**：阅读体验优先

### 5.2 验证方法论（browser-use MCP）

- 改完必须先刷 SW 再测：`registration.update()` → 等 1.5s → **reload 两次**，否则测的是旧代码
- `take_screenshot` 常 15s 超时 → 放弃截图，用 `evaluate_script` 读数值（scrollY、DOM、localStorage、IndexedDB）验证
- annotate.js 启动时缓存 localStorage，外部直写数据后需 reload 才可见
- 模拟贴图可用 `new ClipboardEvent('paste', {clipboardData})`（Chrome 支持）

### 5.3 环境与工具坑

- Python 一律 `python -X utf8`（中文路径/输出乱码）
- PowerShell 用 `;` 分隔命令，无 `&&`、无 `head`
- git push 需临时代理 `http://127.0.0.1:7890`，连接重置先确认 VPN 开启
- 文件工具返回状态不可信：SearchReplace 可能假报失败、DeleteFile 可能假报成功——一律 Grep/脚本二次验证
- 内联 `python -c` 含中文+嵌套引号易被 shell 破坏 → 写临时 .py 执行后清理

### 5.4 协作模式经验

- 大改动先评可行性、给方案对比，用户拍板后再动手（贴图功能即此流程）
- 需求会转向（导图→卡片→阅读站），架构保持简单（纯静态零依赖）让每次转向成本极低
- 内容建设的工作量远大于代码：资料整合、考纲裁剪、错误修正占了大半轮次
- 用户交代过的体验原则要沉淀成文档（skill/FEATURES.md），避免后续改动回退

---

## 六、最终交付清单

| 交付物 | 位置 |
|---|---|
| 笔记站 PWA（离线可用、可安装） | `math-note\pwa\` → GitHub Pages 在线 |
| 笔记内容源 | `math\导图\`（17 份）+ `math\好题\`（36 份） |
| 构建/工具脚本 | `tools\build_notes.py`、`fetch_katex.py`、`make_icon.py` 等 |
| 产品功能总结 | `math-note\FEATURES.md` |
| 通用笔记维护 skill | `C:\Users\cjx\.qoder\skills\note\SKILL.md`（用户级） |
| 本文档 | `math-note\开发全流程总结.md`（`D:\ai code\` 下另存一份） |
