# 精选选填功能 实施计划

> 状态：规划中（待确认「答案处理方案」后开工）
> 创建：2026-08-21

---

## 1. 目标

在笔记站 `index.html` 左侧栏「考研数学笔记」标题与 🌙 按钮**之间**，加一个胶囊按钮 **「✨ 精选选填」**。
点击后**新开页面** `selected.html`，按套卷展示模拟卷选填真题（选择题 + 填空题），每题支持：**查看答案 / ⭐收藏 / 笔记**（与真题系统 `exam.html` 完全一致的能力）。

---

## 2. 数据源（已核实）

### 2.1 题目源：`数二模拟卷选填精选.pdf`（94 页，**有文字层**）
通过 pymupdf 扫描「选择题」页定位，精选 PDF 实际含 **31 套**（每套间隔 3 页）：

| 套卷组 | 套数 | 起始页 |
|--------|------|--------|
| 24 余炳森五套卷 | 5 | p1,4,7,10,13 |
| 24 李林四套卷 | 4 | p16,19,22,25 |
| 24 张宇四套卷 | 4 | p28,31,34,37 |
| 24 超越十套卷（=合工大超越） | 10 | p40,43,…,67 |
| 25 张宇八套卷 | 8 | p70,73,…,91 |

每套结构：选择题 10 道（1–10）+ 填空题 6 道（11–16）→ 共约 **31 × 16 = 496 题**。

### 2.2 答案源：`E:\Desktop\做题\数二精选模拟题和答案\` 下 5 个 PDF（**全部为扫描图、无文字层**）

| 套卷组 | 答案 PDF | 内容类型 |
|--------|----------|----------|
| 李林四套卷 | `24李林四套卷 数二 试题+答案.pdf` | 试题+答案同册 |
| 张宇四套卷 | `24张宇四套卷答案-数二.pdf` | 纯答案 |
| 张宇八套卷 | `25考研张宇八套卷（数二）.pdf` | 完整试题+解析 |
| 余炳森五套卷 | `余炳森五套卷数二.pdf` | 完整试题+解析 |
| 合工大超越 | `24合工大超越卷解析篇-数二.pdf` | 纯解析 |

> ⚠️ 前 3 页文本长度测均为 **0** → 确认全部是扫描图。**答案无法用文字解析**，必须走「图片 / 视觉」路线。

---

## 3. 关键约束 / 风险

1. **答案 PDF 全为扫描图（无文字层）** → 答案不能像真题那样从文字 PDF 解析成 JSON 文本，只能渲染成图片或靠视觉模型 OCR。
2. **题目 PDF 数学符号严重乱码**（已抽样：如 `f x  `、`∫` 错乱、矩阵 `` 乱码、`1/2` 排版错位）→ 提取后需**逐套渲染 PNG 视觉核对修正**，工作量与真题 2014–2025 核对相当。
3. **题量巨大**：496 道选填，逐题核对成本高。建议分批（先 1–2 套跑通全链路，再批量）。
4. **答案 PDF 部分「题+答案混排」**（李林/张宇八套/余炳森均含试题）→ 定位答案时必须**跳过题目区、精准切到答案/解析区**。

---

## 4. 题目提取方案（`tools/build_selected.py`）

- 用 pymupdf 按套卷定位页范围，提取每题：
  - 选择题：题干 + 选项 A–D
  - 填空题：题干 + 空
- 结构化输出 `pwa/data/selected.json`，字段对齐真题：
  ```json
  {
    "papers": [
      { "id":"y24-ybs-1", "title":"24 余炳森五套卷·卷一",
        "sections":[ {"type":"choice","questions":[ {no:1, stem:"...", options:["A...","B..."], kind:"choice"}, ... ]},
                     {"type":"fill","questions":[ {no:11, stem:"...", kind:"fill"}, ... ]} ] }
    ]
  }
  ```
- 提取后逐套渲染关键页 PNG，对照 md/文本做**视觉核对修正**（参照真题管线）。
- 跑 `tools/_katex_check.js` 做公式校验（覆盖 stem/options）。

---

## 5. 答案处理方案（✅ 已确认：方案 B — OCR 成文字答案）

> 用户确认：渲染答案页 → 视觉模型/Read 识别每题答案选填答案 → 写入 JSON 文本（如 `1.D 2.B … 11. 1/2 …`）。
> 点「查看答案」→ 显示文字答案。需逐套核对，填空题尤需谨慎。

- 建立「套卷 → 答案 PDF + 答案页」映射（渲染答案 PDF 逐页定位，跳过题目区精准切到答案区）。
- 渲染该套卷答案页 PNG → 视觉识别每题答案 → 写入 `selected.json` 的 `answer` 字段。
- ⚠️ 李林/张宇八套/余炳森答案 PDF 含试题，须跳过题目区。

---

## 6. 文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `tools/build_selected.py` | 新增 | 题目提取脚本 |
| `pwa/data/selected.json` | 新增（入库） | 题目结构 |
| `pwa/data/selected_ans/*.png` | 新增（若选 A/C） | 答案页图片 |
| `pwa/selected.html` | 新增 | 页面骨架，复用 exam.css |
| `pwa/selected.js` | 新增 | 从 `exam.js` 复制 `qCard/toggleQSec/toggleFav/noteGet/noteInput/mdBlock/mdInline` 等，改数据源为 `selected.json` |
| `pwa/css/exam.css` | 复用 | 题卡/答案/笔记/收藏样式全部复用 |
| `pwa/index.html` | 改动 | `side-head` 加胶囊按钮「✨ 精选选填」(target=_blank 打开 selected.html) |
| `pwa/sw.js` | 自动 | pre-commit 钩子 bump CACHE_VER |

> 题目 md 源同真题一样放仓库外（`E:\Desktop\做题\…`），git 仅入库构建产物 `selected.json` + 答案 PNG。

---

## 7. 实施步骤（建议顺序）

1. **先确认答案方案（第 5 节）**。
2. 写 `build_selected.py`，先跑 **1 套**（如余炳森卷一）验证提取质量。
3. 视觉核对该套题目，修正乱码。
4. 按选定答案方案处理该套答案。
5. 建 `selected.html` + `selected.js` + `index.html` 入口，跑通单套全链路。
6. 批量扩展到 31 套，逐套视觉核对（可 6 Agent 并行）。
7. KaTeX 校验 0 失败 → 提交推送。

---

## 8. 待确认 / 开放问题

- [ ] **答案处理方案**：A / B / C？（决定答案管线形态）
- [ ] 答案 PNG 若入库，体积是否可接受（建议 150dpi + 仅答案页）；或改放本地不入库只随部署？
- [ ] 套卷顺序/分组是否按上文 5 组展示（余炳森/李林/张宇四套/超越/张宇八套）？
- [ ] 是否需要在精选页也加「只看收藏」开关（同真题）？
