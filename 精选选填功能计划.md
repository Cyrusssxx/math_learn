# 精选选填功能 实施计划

> 状态：规划中（待确认「答案处理方案」后开工）
> 创建：2026-08-21

---

## 1. 目标

在笔记站 `index.html` 左侧栏「考研数学笔记」标题与 🌙 按钮**之间**，加一个胶囊按钮 **「✨ 精选选填」**。
点击后**新开页面** `selected.html`，按套卷展示模拟卷选填真题（选择题 + 填空题），每题支持：**查看答案 / ⭐收藏 / 笔记**（与真题系统 `exam.html` 完全一致的能力）。

---

## 2. 数据源（已核实）

### 2.1 题目源：`数二模拟卷选填精选.pdf`（94 页，**有文字层**）
通过 pymupdf 扫描「选择题」页定位，精选 PDF 实际含 **31 套**（每套间隔 3 页）：

| 套卷组 | 套数 | 起始页 |
|--------|------|--------|
| 24 余炳森五套卷 | 5 | p1,4,7,10,13 |
| 24 李林四套卷 | 4 | p16,19,22,25 |
| 24 张宇四套卷 | 4 | p28,31,34,37 |
| 24 超越十套卷（=合工大超越） | 10 | p40,43,…,67 |
| 25 张宇八套卷 | 8 | p70,73,…,91 |

每套结构：选择题 10 道（1–10）+ 填空题 6 道（11–16）→ 共约 **31 × 16 = 496 题**。

### 2.2 答案源：`E:\Desktop\做题\数二精选模拟题和答案\` 下 5 个 PDF（**全部为扫描图、无文字层**）

| 套卷组 | 答案 PDF | 内容类型 |
|--------|----------|----------|
| 李林四套卷 | `24李林四套卷 数二 试题+答案.pdf` | 试题+答案同册 |
| 张宇四套卷 | `24张宇四套卷答案-数二.pdf` | 纯答案 |
| 张宇八套卷 | `25考研张宇八套卷（数二）.pdf` | 完整试题+解析 |
| 余炳森五套卷 | `余炳森五套卷数二.pdf` | 完整试题+解析 |
| 合工大超越 | `24合工大超越卷解析篇-数二.pdf` | 纯解析 |

> ⚠️ 前 3 页文本长度测均为 **0** → 确认全部是扫描图。**答案无法用文字解析**，必须走「图片 / 视觉」路线。

---

## 3. 关键约束 / 风险

1. **答案 PDF 全为扫描图（无文字层）** → 答案不能像真题那样从文字 PDF 解析成 JSON 文本，只能渲染成图片或靠视觉模型 OCR。
2. **题目 PDF 数学符号严重乱码**（已抽样：如 `f x  `、`∫` 错乱、矩阵 `` 乱码、`1/2` 排版错位）→ 提取后需**逐套渲染 PNG 视觉核对修正**，工作量与真题 2014–2025 核对相当。
3. **题量巨大**：496 道选填，逐题核对成本高。建议分批（先 1–2 套跑通全链路，再批量）。
4. **答案 PDF 部分「题+答案混排」**（李林/张宇八套/余炳森均含试题）→ 定位答案时必须**跳过题目区、精准切到答案/解析区**。

---

## 4. 题目提取方案（`tools/build_selected.py`）

- 用 pymupdf 按套卷定位页范围，提取每题：
  - 选择题：题干 + 选项 A–D
  - 填空题：题干 + 空
- 结构化输出 `pwa/data/selected.json`，字段对齐真题：
  ```json
  {
    "papers": [
      { "id":"y24-ybs-1", "title":"24 余炳森五套卷·卷一",
        "sections":[ {"type":"choice","questions":[ {no:1, stem:"...", options:["A...","B..."], kind:"choice"}, ... ]},
                     {"type":"fill","questions":[ {no:11, stem:"...", kind:"fill"}, ... ]} ] }
    ]
  }
  ```
- 提取后逐套渲染关键页 PNG，对照 md/文本做**视觉核对修正**（参照真题管线）。
- 跑 `tools/_katex_check.js` 做公式校验（覆盖 stem/options）。

---

## 5. 答案处理方案（✅ 已确认：**方案 A — 答案页图片直显**）

> **实测决策（2026-08-22）**：5 个答案 PDF 全为扫描图（无文字层、无书签），OCR 文字方案不可行。
> 用户确认改用 **方案 A**：渲染每套卷的答案/解析页为 PNG，点「查看答案」直接展示原图。

- 每套卷答案页渲染为 `pwa/data/selected_ans/{id}_{n}.png`，写入 `selected.json` 的 `answer_img` 数组。
- selected.js 点「查看答案」→ 展示该套全部答案图（含完整解析过程），100% 准确零转录错误。
- ✅ **已落地**：31 套共 250 张答案图（dpi 120–140），映射完整，0 缺失 0 损坏。

---

## 6. 文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `tools/build_selected.py` | 新增 | 题目提取脚本 |
| `pwa/data/selected.json` | 新增（入库） | 题目结构 |
| `pwa/data/selected_ans/*.png` | 新增（若选 A/C） | 答案页图片 |
| `pwa/selected.html` | 新增 | 页面骨架，复用 exam.css |
| `pwa/selected.js` | 新增 | 从 `exam.js` 复制 `qCard/toggleQSec/toggleFav/noteGet/noteInput/mdBlock/mdInline` 等，改数据源为 `selected.json` |
| `pwa/css/exam.css` | 复用 | 题卡/答案/笔记/收藏样式全部复用 |
| `pwa/index.html` | 改动 | `side-head` 加胶囊按钮「✨ 精选选填」(target=_blank 打开 selected.html) |
| `pwa/sw.js` | 自动 | pre-commit 钩子 bump CACHE_VER |

> 题目 md 源同真题一样放仓库外（`E:\Desktop\做题\…`），git 仅入库构建产物 `selected.json` + 答案 PNG。

---

## 7. 实施步骤（建议顺序）

1. **先确认答案方案（第 5 节）**。
2. 写 `build_selected.py`，先跑 **1 套**（如余炳森卷一）验证提取质量。
3. 视觉核对该套题目，修正乱码。
4. 按选定答案方案处理该套答案。
5. 建 `selected.html` + `selected.js` + `index.html` 入口，跑通单套全链路。
6. 批量扩展到 31 套，逐套视觉核对（可 6 Agent 并行）。
7. KaTeX 校验 0 失败 → 提交推送。

---

## 8. 实施进度（实时更新）

| 阶段 | 状态 | 说明 |
|------|------|------|
| 计划 & 入口 & 余炳森卷一样本 | ✅ | 计划文档 + 胶囊按钮 + selected.html/js + 余炳森卷一（含答案 OCR） |
| 31 套页映射 & 题目 PNG 渲染 | ✅ | 每套 3 页 PNG + `manifest_q.json` |
| 30 套题目视觉转写 | ✅ | 7 Agent 并行，每套 10 选择 + 6 填空 = 16 题 |
| 题目合并入 `selected.json` | ✅ | 共 31 套（含卷一），每套 16 题 |
| KaTeX 校验（selected.json） | ✅ | 新增 `_katex_check_sel.js`，2056 公式 0 失败 |
| 转写错误修复 | ✅ | 18 处 cases/pmatrix 分隔符误写 + 14 处层叠转义修复，0 残留 |
| `selected.js` 容错 | ✅ | `answer=null` 时显示「答案整理中…」，不崩溃 |
| **30 套答案提取 & 入库** | ✅ | 方案 A：250 张答案图 + answer_img 映射，commit `82fe9d8` |
| 提交推送 | ✅ | commit `82fe9d8` 已 push，远程 HEAD 一致 |

## 9. 答案处理方案 · 实测与决策（关键）

> ⚠️ **实测（2026-08-22）**：5 个答案 PDF **全部为扫描图**——
> - 文字层提取为空（前 3 页文本长度均 0）
> - `get_toc()` 书签均为 0 条（**无法程序定位套卷页码**）
> - 答案为**逐题解析**形式（如余炳森卷一需读 p7–p12 解析页提取），**无集中「参考答案」汇总页**
>
> 因此原方案 **B（OCR 成文字答案）成本极高**：每套需读 2–4 页解析扫描图、逐题 OCR，填空题数字/分式极易错；与当初"快速 OCR"假设不符。
>
> **建议改为 方案 A（答案页图片直显）**：渲染每套卷的答案/解析页为 PNG 存入 `pwa/data/selected_ans/`，点「查看答案」直接展示原图——100% 准确、零转录错误，且含完整解析过程。代价：图片占体积、不可文字搜索。
> 或 **方案 C（混合）**：OCR 文字答案快速查看 + 原图可点开核对。
>
> **✅ 已确认方案 A（答案页图片直显）。31 套答案已渲染为 PNG 存入 `pwa/data/selected_ans/`，selected.js 点「查看答案」展示原图（含完整解析）。**

## 10. 待确认 / 开放问题

- [x] 答案处理方案：原选 B 实测不可行 → **✅ 已确认改 A（答案页图片直显）**
- [x] 答案 PNG 入库体积：250 张 PNG，总计约 6MB（dpi 120–140）
- [x] 套卷分组展示：余炳森 / 李林 / 张宇四套 / 超越 / 张宇八套（✅ 已落地）
- [x] 精选页「只看收藏」开关（✅ 已随 v1 实现，含收藏计数与 empty-tip）

---

## 11. 最终验收报告（2026-08-22 浏览器级验收）

### 验收方法

用 **jsdom + 项目自带 katex 0.16.11/auto-render** 复刻 `selected.js` 真实渲染链路：
```
mdBlock/mdInline/balanceDollars → innerHTML → renderMathInElement(throwOnError:false)
```
对全部 496 题 stem+options+answer 文本字段逐一渲染，检测两类用户可见错误：
1. **红色 `#cc0000`** — KaTeX 对非法公式（未知命令/语法错）产出的红色文本或 `.katex-error`
2. **残留 `$`** — auto-render 未渲染的原样 LaTeX 文本

### 验收结果

| 维度 | 结果 |
|------|------|
| 题目总数 | 496（31 套 × 16 题） |
| KaTeX 渲染块数 | 3340（stem 2056 + 选项 1284 + answer 若干） |
| **红色错误 #cc0000** | **0** ← 无未知命令/语法错误 |
| **残留 $** | **0** ← 无漏渲染的原始 LaTeX |
| 数据完整性 | 全部完整（no/kind/stem/options 字段齐备，310 选择题均 4 选项） |
| 答案图 | 250 张 PNG，0 缺失 0 损坏，映射准确 |
| 选项重复 | 3 处（余炳森卷一 Q8 C/D、卷三 Q4 A/B、卷五 Q8 C/D）— **均为原题印刷重复，非转录错误** |

### 灵敏度验证

注入已知非法公式 `\zzzunknown{x}` 到真实题干 → 完整管线检出红色 `#cc0000=true`。确认测试非假阴性。

### 结论

**精选选填功能完全正确，可正常使用。** 渲染链路无可见错误，答案图映射准确，数据完整。
