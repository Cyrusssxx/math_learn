# 考研数学笔记站 · 缺点评估与改进清单

> 评估时间：2026-07　|　评估对象：`math-note`（PWA，SW 缓存 v20）
> 评估方式：通读核心代码 reader.js / annotate.js / common.js / index.html / sw.js
> 说明：本文档只做记录，暂不改动代码；按严重程度分级，每条对应具体代码位置

---

## 一、致命级：批注数据脆弱（最大隐患）

### 1.1 改笔记 = 丢批注，且静默丢失
- **位置**：`annotate.js` `hash()` / `ownText()` / `keyBlocks()`（L48-69）
- **问题**：标注锚点 key = `hash(ownText(block))`（块内文本哈希）。只要那一行 md 改动一个字，重建 notes.json 后哈希变化，该行的荧光/整块高亮/📝批注**全部失效**——数据仍在 localStorage 但永远匹配不上，成为孤儿数据，且无任何提示。
- **冲突点**：产品核心工作流就是"持续补充完善笔记内容"，内容维护与批注保全直接冲突，越用越易积累孤儿批注。
- **改进方向**：build 时为每个块生成稳定 ID（写入 json，而非纯文本哈希）；或改笔记后做一次批注迁移/孤儿检测并提示用户。

### 1.2 重复文本块的批注会漂移
- **位置**：`annotate.js` `keyBlocks()`（L62-69）
- **问题**：文本相同的块用 `hash#序号` 靠 DOM 顺序编号。在两个相同文本块之间插入/删除内容后序号错位，批注漂移到错误的行。
- **改进方向**：同上，稳定 ID 方案可一并解决。

### 1.3 localStorage 无容量兜底
- **位置**：`annotate.js` `save()`（L12）
- **问题**：直接 `setItem` 无 try/catch。批注写满约 5MB 上限时抛 `QuotaExceededError`，新批注保存失败且毫无提示。唯一救命是手动"导出标注"，忘记或换设备/清缓存就全部丢失，无自动备份、无同步。
- **改进方向**：`save()` 加 try/catch + 超限提示；加自动定期导出/提醒。

---

## 二、搜索功能局限

### 2.1 只支持子串完全匹配
- **位置**：`reader.js` `doSearch()`（L286-317，`includes(q)`）
- **问题**：多关键词（"特征值 相似"）搜不到；无分词、模糊、拼音、繁简；数学内容只能靠中文命中（不会有人搜 `\lambda`）。

### 2.2 每次击键全量扫描 + 反复解析 localStorage
- **位置**：`reader.js` `doSearch()`（每次输入都遍历 searchIndex 并 `JSON.parse(localStorage)`）
- **问题**：无 debounce，数据一大就输入卡顿。
- **改进方向**：加 debounce（150-250ms）；批注索引缓存，不每次 parse。

### 2.3 结果与定位都不高亮关键词
- **位置**：`reader.js` `doSearch()` 结果渲染（L309-316）/ `locateBlock()`（L335-349）
- **问题**：结果列表只 `slice(0,60)` 截断，定位到正文只整块闪烁，命中词本身不高亮，长条目里难一眼找到。

---

## 三、渲染层：自造 Markdown 解析器不健壮

### 3.1 语法子集受限
- **位置**：`reader.js` `mdToHtml()`（L139-186）/ `emitList()`（L97-113）
- **问题**：不支持 `###` 三级标题（被当普通段落）、有序列表、代码块、引用；列表缩进硬编码按 2 空格算 level，Tab 或 4 空格会层级错乱。约束全靠人肉遵守，`build_notes.py` 只校验文件名不校验语法。

### 3.2 KaTeX 缺失时降级为源码
- **位置**：`reader.js` `renderMath()`（L435-444）
- **问题**：`renderMathInElement` 不存在时跳过，公式显示为原始 `$...$`，纯公式笔记基本不可读；与 reader.js 均 defer，靠加载顺序保证，较隐蔽。

### 3.3 图片 src 未过滤
- **位置**：`reader.js` `inline()`（L55-60，`$2` 直进 src）
- **问题**：未过滤 `javascript:` 等。内容自己可信，风险低但不严谨。

---

## 四、工程 / 架构

### 4.1 notes.json 单文件全量加载
- **位置**：`reader.js` `init()`（L14-22）
- **问题**：一次 fetch 全部进内存 + 全量建索引。现约 50 份尚可，规模大后首屏与内存明显上升，无按需加载。

### 4.2 发布链路全靠手动、极易漏步
- **位置**：`sw.js` `CACHE_VER`（L5）+ 手动 build/验证流程
- **问题**：改 md→手动 build→**手动 bump CACHE_VER**→手动验证。忘记 +1 就发旧缓存（skill 反复强调正说明这是高频人为失误点）。无 CI、无自动化测试（`tools/test_fsrs.js` 是废弃卡片系统遗留）。
- **改进方向**：build 脚本自动写版本号（时间戳/内容 hash），从根上消除人为失误。

### 4.3 代码风格不统一 + 全局污染
- **位置**：`reader.js`（裸全局函数 + 内联 `onclick="return locateHit(${i})"`）vs `annotate.js`（IIFE）
- **问题**：全局命名空间污染，内联事件不利于加 CSP。

---

## 五、交互 / 体验

- **无"上次读到哪"记忆**：`route()` 刷新回 notes[0]；计时器只累计总时长不记进度（`common.js`）。学习类应用较重要。
- **移动端/触屏批注基本不可用**：选词批注靠 `mouseup`+`getSelection`（`annotate.js` initBar L318-332），触屏长按选择体验差；工具条 `rect.top-46` 窄屏易顶出屏幕。
- **单击放大的小副作用**：图片 click 里 `stopPropagation`（`reader.js` L429-432）会挡住 annotate 的 document click，点图片时已弹出的批注工具条不会收起。
- **lightbox 太简陋**：只放大到 95vw，不能捏合缩放看细节、无多图切换。
- **可访问性缺失**：`<div onclick>` 当按钮、搜索结果不能键盘上下选、无 aria、无焦点管理。

---

## 六、定位层面

- **号称"通用笔记"实则处处写死**：`SUBJECT_NAMES`、文件名正则、数二考纲、仓库地址、代理端口全硬编码，别人几乎无法直接复用。
- **纯静态无后端**：多设备标注不同步（只能手动导出/导入 JSON），无协作、无版本历史。

---

## 优先级建议（若只修三个）

| 优先级 | 问题 | 为何优先 | 改进方向 |
|---|---|---|---|
| P0 | 批注锚点脆弱（1.1 / 1.2） | 会实际伤害自己的标注数据 | build 时生成稳定块 ID 写入 json；或加孤儿批注检测提示 |
| P0 | localStorage 写满静默失败（1.3） | 数据丢失且无感知 | `save()` 加 try/catch + 提示 + 自动定期导出 |
| P1 | CACHE_VER 手动 bump 易漏（4.2） | 高频人为失误、影响所有用户 | build 脚本自动写版本号 |
