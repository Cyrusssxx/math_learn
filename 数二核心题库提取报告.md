# 数二核心题库提取报告

> 数据源：`D:/ai code/【A4紧凑】数二大观严选题做题本.pdf`（119 页）
> 容器：`tools/yancai_bank.json` ｜ 更新：2026-09-17

---

## 一、总体进度

| 范围 | 题数 | 来源方式 | status |
|---|---|---|---|
| p11–p90 | 445 | 视觉子代理逐页转写 LaTeX | `transcribed` |
| p91–p119 | 152 | PDF 文字层切块 + 参考库精确匹配 | `ref-matched` |
| p91–p119 | 9 | 待视觉/人工复核 | `needs-visual` |
| **合计** | **606** | | |

前 10 页为封面 + 序言 + 统计 + 6 页目录，无题目。

---

## 二、两个关键发现（改变了整条技术路线）

### 1. 这份 PDF **不是扫描件，带完整文字层**
`page.get_text()` 能直接取到**权威字符**（第 17 页 733 字符、0 张图片）。
文字层被 MathType 打散（分式/根号/积分上下限的结构丢失），但**数字、字母、中文、题源标签是可靠的**：

```
(a.a.b.a.h.b.b)2013 数三
设曲线 y = f(x) 与 y = x² − x 在点 (1,0) 处有公共切线, 则 lim n→∞ n f(n/(n+2)) =
```

→ 之前被判定为「扫描件分辨率不足、看不清」的一系列【不确定】项，**大部分可直接定案**。

### 2. 本地参考题库可覆盖**数一/数二/数三真题 1987–2026**
`大观园 assets/questions.json`（5952 题，含答案+解析，带 `source` 标注），加上：

| 参考库 | 题数 | 覆盖 |
|---|---|---|
| 大观园 questions.json | 5952 | 数一/二/三 1987–2026 |
| exam.json | 607 | 数二 2000–2026 |
| practice.json | 1402 | 大观园已导入版 |
| bank_questions.json | 827 | 数二为主 |
| **合计** | **8788** | |

→ **「数一数三去网上复核」已被更优路径替代**：本地参考库可做全量交叉验证，比逐题搜网更可靠、更快。

---

## 三、交叉核对结果

对 445 道视觉转写题，用 8788 条参考题做全量比对（3-gram 倒排索引粗筛 + 序列相似度精排）：

| 题源 | 数量 | ✅ 一致 | 🟡 有差异 | 🟠 中相似 | ❌ 未匹配 |
|---|---|---|---|---|---|
| 数一 | 144 | 120 | 17 | 3 | 4 |
| 数二 | 210 | 189 | 14 | 6 | 1 |
| 数三 | 86 | 70 | 13 | 2 | 1 |
| 其他 | 5 | 2 | 2 | 1 | 0 |

再用 PDF 文字层做**字符级双向校验**（核心有而 PDF 无 = 多字/错字；PDF 有而核心无 = 漏字）。

---

## 四、已确认并修正的 17 处错误

每条均有证据（PDF=文字层原文 / DGY=大观园参考库 / MATH=数学自洽）。

| # | 题号 | 原（错） | 改为 | 证据 |
|---|---|---|---|---|
| 1 | yc-14-1 · 1997数二 | `lim_{x→0}` | `lim_{x→-∞}` | PDF；且 x→0 时根号内 4x²+x−1<0 无意义 |
| 2 | yc-14-6 · 2026数三 | `√((1+x²)/sin x)` | `(√(1+x²))/sin x` | DGY：根号只盖 1+x² |
| 3 | yc-17-2 · 2013数一 | 漏系数 `n` | `lim n·[f(1/n)−1]` | PDF；缺 n 则极限为 0（答案 1） |
| 4 | yc-17-3 · 2013数三 | 漏系数 `n` | `lim n·f(n/(n+2))` | PDF + DGY（答案 −2） |
| 5 | yc-17-4 · 2024数二 | `y=e^{2t}` | `y=e^{t²}` | PDF；e^{2t} 得 4e²/3 与选项不符 |
| 6 | yc-18-1 · 2016数一 | 缺因子 `t`、分母 `1−cos²t` | `∫₀ˣ t·ln(1+t sin t)dt / (1−cos x²)` | PDF + DGY（答案 1/2） |
| 7 | yc-18-3 · 2000数二 | `∫₀ˣ cos t dt` | `∫₀ˣ\|cos t\|dt` | 2000数二真题原文；结论 2n≤S(x)<2(n+1) 只有 \|cos t\| 成立 |
| 8 | yc-24-1 · 1998数二 | `∫₀ˣ` | `∫_bˣ` | PDF + DGY；题中要求确定 a,b,c |
| 9 | yc-30-2 · 2020数二三 | `e^{1/x}` | `e^{1/(x−1)}` | DGY |
| 10 | yc-31-5 · 2022数二 | `f(eˣ)` | `f(e^{x²})` | DGY（答案 −1）；用 eˣ 时极限发散 |
| 11 | yc-58-7 · 2010数一 | `∫₀^π` | `∫₀^{π²}` | DGY；答案 −4π 只有 π² 成立 |
| 12 | yc-61-2 · 1993数二 | `∫₀^{π/2}` | `∫₀^{π/4}` | DGY（答案 π/8−¼ln2）；π/2 时发散 |
| 13 | yc-81-4 · 2024数三 | `∫_a^{a+2π}` | `∫_a^{a+kπ}` | PDF + DGY；否则「k 为整数」无意义 |
| 14 | yc-82-4 · 2020数二 | 第三支 `x, x=0` | `y, x=0` | PDF 文字层分段函数原文 |
| 15 | yc-85-4 · 2022数二三 | 选项 B 无负号 | `∂²F/∂x² = −∂²F/∂y²` | PDF 文字层 |
| 16 | yc-88-4 · 2025数三 | `∫_y^x x·e^{−y²}dt` | `∫_y^x e^{−t²}dt` | PDF 文字层 |
| 17 | yc-64-3 · 1995数三 | 裸 `\n` 未换行 | 还原为换行 | 结构校验 |

同时**清除**已定案题目的 8 处【不确定】标注；并确认 **yc-11-3（2020数三）选项正确**（两个独立来源一致，无需修改）。

---

## 五、附带发现：exam.json（你自建数二真题库）的一处缺陷

**2003 数二 第 11 题**选项 D 被写坏：

```
现状： (D) $1>\dfrac{38}{15}>I_{2}>I_{1}$      ← "38/15" 疑为 LaTeX 解析残留
应为： (D) $1>I_{2}>I_{1}$
```

依据：答案 (B) `1>I₁>I₂`，(D) 应与之仅 I₁/I₂ 顺序互换；且 `1 > 38/15` 本身不成立。

---

## 六、工具链（`tools/`，可复用到其他题库项目）

| 脚本 | 作用 |
|---|---|
| `_extract_textlayer.py` | 抽取 PDF 文字层 → `_yancai_textlayer.json` / `.txt` |
| `_match_ref.py` | 核心题库 ↔ 8788 参考题全量交叉核对；`show <id...>` 查指定题同源候选 Top8 |
| `_verify_textlayer.py` | 用文字层做字符级双向校验 → `_tl_report.txt` |
| `_extract_rest.py` → `_rest_draft.json` → `_reintegrate_rest.py` | p91–119 重建管线（文字层切块 → 参考库取 LaTeX → 字符互校 → 入库） |
| `_validate_yancai.py` | LaTeX 结构校验（配平/宏白名单/污染） |
| `_rebuild_bank.py` | 修正批次文件反斜杠风格差异，重建题库 |

---

## 七、待办

1. **p91–119 的 9 题**（二重积分/微分方程为主）需视觉复核或人工定稿：
   `yc-99-1、yc-99-4、yc-100-2、yc-100-4、yc-102-3、yc-109-2、yc-109-4、yc-110-4、yc-113-5`
2. **catId 映射**：按 PDF 子标签映射到 `exam_categories.json` 的 L2 节点（映射关系已验证可行）。
3. **接线分类页**：生成 `pwa/data/core_bank.json`，`category.js` 支持加载并按现有知识点树筛选（按钮已改为「📘 核心题库筛选」）。
4. 视觉子代理受频率限制（2026-09-18 00:15 重置），但新管线已基本不需要子代理。

---

## 八、修正示例（对照留档）

**yc-18-1（2016数一）** — PDF 文字层原文：
```
(a.a.b.a.i.a.c)2016 数一
lim x→0   ∫₀ˣ t·ln(1+tsint) dt / (1−cos x²)  = _____
```
原有转写漏了被积函数的因子 `t`，且把分母写成 `1−cos²t`。

**yc-88-4（2025数三）** — PDF 文字层原文：
```
已知函数 z=z(x,y) 由 z + lnz − ∫_y^x e^{−t²} dt = 1 确定，则 ∂²z/∂x²|_(1,1) = _____
```
原有转写把被积函数误读为 `x·e^{−y²}`。
