# 考研数学笔记站（math-note PWA）· 缺点评估报告

> 评估对象：`d:\ai code\math-note`（考研数学笔记阅读 PWA）
> 评估方式：通读 `index.html`、`js/reader.js`、`js/annotate.js`、`js/common.js`、`sw.js`、`tools/build_notes.py` 及 `pwa/data/notes.json` 结构
> 数据规模：51 篇笔记，单文件 `notes.json` 约 583KB / 约 40 万字符
> 生成时间：2026-07-27

---

## 一句话总结

当前体量下体验流畅、离线完备，批注/搜索/夜间/计时等功能齐全，工程质量在个人项目里算高。但有**两个会真实咬人的坑**必须优先修：① 批注锚点绑定"文本哈希"，改内容即丢标注；② localStorage 存批注且无配额保护，写满即静默丢数据。其余为"现在够用、规模上去要还债"的技术债。

---

## 🔴 严重问题

### 1. 批注锚点太脆：改笔记就丢标注（最高危）

`annotate.js` 用**块内文本的 djb2 哈希**作为锚点：

```js
// keyBlocks()
el.dataset.ak = n > 1 ? h + '#' + n : h;   // h = hash(ownText(el))
```

**后果**：

- 只要编辑源笔记里某一行（哪怕修个错字、加个标点），该行哈希即变 → 挂在其上的**荧光高亮 / 整块着色 / 文字批注全部静默消失**（恢复时 `byKey[m.k]` 找不到就跳过）。而这是一个**会频繁增改内容**的笔记站，冲突极大。
- 丢失的批注**永久残留在 localStorage**，不显示也不回收（无 GC 机制）。删除/改名笔记同理，数据成孤儿。
- 重复相同文本的块靠"出现次序 `#n`"区分（`h + '#' + n`），**调整段落顺序也会串位**。

> 对比：英语二 PWA 用 `data-sid`（稳定句子 ID）锚定，无此问题。此处因源 md 没有稳定块 ID，只能退化为哈希文本。

**建议改进**：`build_notes.py` 为每个列表项/段落生成稳定 ID（如 `章节序号+块序号`），前端锚点改用 ID；并增加"孤儿批注"检测→迁移/清理。

### 2. localStorage 存批注，无配额保护

- 批注文本 + 全部 `hl`/`marks`/`notes` 塞在单个 localStorage key（`notesAnnot`），上限约 5MB；笔记多、批注多时会触顶。
- `save()` 直接 `setItem`，**无 try/catch**。一旦 `QuotaExceededError`，批注静默保存失败、用户无感知（违背"写不进要立即告知用户"的既定原则）。

**建议改进**：`save()` 包 try/catch，捕获配额异常并提示用户导出备份/清理；或将批注整体迁到 IndexedDB（图片已在 IndexedDB）。

---

## 🟠 中等问题

### 3. 整站数据一次性全量加载 + 缓存粒度粗

- `init()` 一次 `fetch('data/notes.json')` 把 583KB / 40 万字全读进内存并立即 `buildSearchIndex()` 建全量索引。现在能扛，但**随笔记增长线性变差**，首屏与搜索均无分片/懒加载。
- `notes.json` 进 SW 预缓存。**改任一篇笔记都要 bump `CACHE_VER`（现 `mathcards-v20`）且整包 583KB 重新下载**——未按篇/科目拆分，缓存命中率低。

**建议改进**：按科目或按篇拆分数据文件，路由时按需加载；搜索索引可预构建为独立文件。

### 4. 手写 Markdown 子集解析器，能力有限且脆

`reader.js` 的 `mdToHtml` 为自研迷你解析器，仅认 `## 标题`、`- 列表`、表格、独行图片、`::: fold`、`**加粗**`、`⭐`。缺失/风险：

- **不支持 `### 三级标题`**（右侧"本页目录"仅到 H2，颗粒度粗）、**不支持有序列表、代码块**。
- **正文不支持超链接** `[文字](url)`——`inline()` 仅处理图片，写链接会原样漏出。
- 列表缩进硬编码"2 空格 = 1 级"（`Math.floor(m[1].length/2)`），Tab 或 4 空格会错乱（虽有跳级容错）。
- **属性未转义引号**：`inline()` 的 `<img src="$2">`、figure 的 `esc(m[2])`——`esc()` 只转义 `& < >`，**不转义 `"`**。图片路径含引号即可破坏属性（单作者内容风险低，但确属注入面）。

### 5. 搜索实现简单

- `oninput` 每次击键全量 `searchIndex.forEach(...includes())` + 重排重渲染，**无防抖**。
- 索引把 `$...$` 内的 **LaTeX 原文也收了进去**，搜"积分"搜不到公式，搜到的正文片段还夹着 `\dfrac{}` 原始码；**数学内容基本搜不动**，也无拼音/模糊匹配。

**建议改进**：加输入防抖；索引构建时剥离/转写公式为可读文本；可选拼音首字母匹配。

### 6. 更新体验 & 缓存策略

- FEATURES 自述"**刷新两次页面才用上新版本**"。SW 已有 `skipWaiting()`+`clients.claim()`，本应一次生效——该说明反映**更新 UX 不顺**：缺"发现新版本→提示/自动刷新"机制。
- fetch 纯 cache-first，同源资源命中后**不做后台校验**；忘了 bump `CACHE_VER`，用户永远看不到更新。

**建议改进**：加 SW `updatefound` → 前端提示"有新版本，点击刷新"；或对 `notes.json` 用 stale-while-revalidate。

---

## 🟡 轻微 / 产品完整度

- **`notes.json` 是单行压缩 JSON**（非 pretty-print）。583KB 一行文件，**git diff 完全不可读**，版本追溯困难。→ 建议输出带缩进或按篇拆分。
- **无数据校验工具**：`build_notes.py` 图片缺失仅打印警告仍保留原引用；不像英语二有 `validate.py` 兜底。内容与 `notes.json` 两步式，易忘重建。
- **路由/锚点耦合中文文件名**：`id = 文件名 stem`，改名同时破坏书签 + 孤儿化批注。
- **阅读计时只有"今日"**，跨天清零，无周/累计/历史统计，激励价值有限。
- **图片灯箱**无缩放/拖动/多图切换；**无打印样式**（数学笔记常需打印）。
- 项目脱胎于 `math-cards`（`tools/test_fsrs.js` 尚在），但阅读站**无间隔重复/自测**功能——"记忆卡片"与"笔记阅读"割裂为两个项目。
- 无笔记级收藏/书签、无"最近阅读"、无跨笔记双链。

---

## 优先级建议

| 优先级 | 事项 | 理由 |
|---|---|---|
| P0 | 批注改用稳定块 ID 锚点（#1） | 改内容即丢标注，是数据丢失级隐患 |
| P0 | `save()` 加配额异常捕获与提示（#2） | 静默丢数据，违背告知原则 |
| P1 | notes.json 按科目/篇拆分 + 懒加载（#3、#6） | 规模增长的天花板 |
| P2 | 搜索防抖 + 公式剥离（#5）、md 解析补 H3/链接（#4） | 体验优化 |
| P3 | notes.json 可读化、加 validate、计时统计等（🟡） | 维护性与产品完整度 |

---

*本报告仅为评估记录，未改动任何代码。如需动手修复，建议从 P0 的批注稳定 ID 入手。*
