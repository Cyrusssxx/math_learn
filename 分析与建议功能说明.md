# 「分析与建议」功能说明

> 真题页（exam.html）与收藏汇总页（fav.html）共享的薄弱点分析面板。
> 入口：两个页面顶部操作栏的 **📊 分析与建议** 按钮（同一组件、同一入口）。

## 一、数据来源

| 数据 | 存储位置 | 说明 |
|---|---|---|
| ⭐ 收藏 | `localStorage['examFav']` | `{ '套卷id-题号': 1 }`，真题页 ☆ 收藏、收藏汇总页收藏 |
| 🟡 不熟 / 🔴 不会 | `localStorage['examStatus']` | `{ '套卷id-题号': 'unfamiliar' \| 'unknown' }` |
| 知识点分类树 | `data/exam_categories.json` | L0 学科 / L1 章 / L2 知识点（分析以 **L2** 为单位） |
| 题目知识点归属 | `data/exam.json` 每题 `categoryIds` | 一题可挂多个知识点 |

**不依赖导出文件**：分析直接读取浏览器内的实时标记（导出 MD 仅是快照，不参与计算）。

## 二、分析依据

1. **聚合**：遍历全部真题，把每道有标记的题按其 `categoryIds` 归入对应知识点（L2），累计该知识点的：

   | 计数 | 含义 |
   |---|---|
   | `unk` | 🔴 不会题数 |
   | `unf` | 🟡 不熟题数 |
   | `fav` | ⭐ 收藏题数 |
   | `total` | 该知识点下的题目总数（题目基数，用于判断覆盖度） |

2. **薄弱分**：`score = unk × 3 + unf × 2`
   - 「不会」权重高于「不熟」；收藏只表示"关注过"，不计入薄弱分（作为参考展示）。
3. **分级**：`score ≥ 6 → 🔴 需加强`；`3 ~ 5 → 🟡 关注`；`1 ~ 2 → 🟢 已跟踪`
4. **排序**：薄弱分降序 → 不会数 → 不熟数 → 收藏数。
5. **建议生成**：按等级 + 计数生成针对性文案，给出**建议练习量**（需加强：`max(6, 不会×3)` 题；关注：`max(4, 不熟×2)` 题；已跟踪：抽查 2~3 题）与动作（重做写出完整步骤 → 连做同类 → 隔天重做错题）。

## 三、交互流程（闭环）

```
真题页/收藏汇总页  →  点「📊 分析与建议」
        ↓
   分析面板（实时计算）
   ├─ 概览：收藏/不熟/不会数、涉及知识点数、三级分布
   └─ 列表（按薄弱分降序）：知识点名 + 路径 + 计数 + 建议 + 【🔁 跳转刷题】
        ↓  点【🔁 跳转刷题】
   category.html?cid=<知识点id>（新标签）
   ├─ 自动选中该知识点节点、展开分类树、列出该知识点的题
   └─ 直接刷题；用 ☆ / 🟡不熟 / 🔴不会 标记
        ↓  回到分析面板，点【🔄 重新分析】
   等级随最新标记实时更新（改善的知识点自动降级为 🟢）
```

- **跳转落地页**：分类页支持 `?cid=` 直达（自动展开树 + 选中节点 + 渲染题目列表）。
- **闭环性**：全程无需重新导出或手动录入；标记变化后一次「重新分析」即可看到新的薄弱点排序与等级。

## 四、界面呈现

| 区域 | 内容 |
|---|---|
| 顶部栏按钮 | 📊 分析与建议（真题页、收藏汇总页各一个，同款同入口） |
| 面板头部 | 标题 + 一句说明（数据来源与依据）+ 关闭 |
| 概览条 | ⭐收藏 N · 🟡不熟 N · 🔴不会 N ｜ 涉及知识点 N 个（🔴需加强 x · 🟡关注 y · 🟢已跟踪 z） |
| 结果行 | 左侧等级色条（红/黄/绿）+ 序号 + 知识点名 + 等级标签 + 分类路径 + 计数与薄弱分 + 训练建议 + 🔁 跳转刷题 |
| 面板底部 | 🔄 重新分析 + 数据来源/依据说明 |

**颜色与标签**：🔴 需加强（红边）/ 🟡 关注（黄边）/ 🟢 已跟踪（绿边）——按优先级一眼区分"仍需加强"的知识点。

## 五、实现文件

| 文件 | 作用 |
|---|---|
| `js/analysis.js` | 分析引擎 + 共享面板（`openAnalysisPanel()` / `analysisBuild()` / `analysisJump(cid)`） |
| `css/analysis.css` | 面板样式（浅色/深色自适应 CSS 变量） |
| `exam.html` / `fav.html` | 顶部栏按钮 + 引入组件 |
| `js/category.js` | 分类页 `?cid=` 直达定位支持 |
| `js/category.js` / `category.html` | 筛选拆分：📥 收藏 / 🟡 不熟 / 🔴 不会 三项独立（与收藏汇总页一致） |

## 六、边界与限制

1. **只统计有标记的题**：完全没被收藏/标记的知识点不会出现在列表（无信号）。
2. **权重为经验值**：`不会×3 + 不熟×2` 是启发式，可按习惯调整（改 `analysis.js` 中一处即可）。
3. **知识点粒度**：统计到 L2（知识点）；若希望按 L1（章）汇总，把 `c.level !== 2` 的判断改为 `c.level !== 1` 即可。
4. **数据仅存本地**：换设备/清浏览器数据后标记会丢失（收藏汇总导出 MD 可作备份）。
