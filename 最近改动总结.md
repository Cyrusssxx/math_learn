# 最近改动总结（真题系统 & 二次型强化）

> 范围：2026-08-20 ~ 2026-08-22
> 提交区间：`9c235c3` → `6b7b513`（main 当前 HEAD）
> 主题：真题公式渲染修复、二次型母题与题型导航、搜索联动、收藏/重定位、真题思路+笔记功能、全量真题数据核对、**真题配图功能**

## 〇-2、真题数据勘误（题库交叉比对后续）
- `abf836d` 2010解答(20) 推导笔误：`cos 4θ` → `cos⁴θ`（最终值不变）
- `e5b2231` 2011选择(7) 选项勘误：(B)(D) 由转置版 $P_1^\mathrm{T}P_2$ / $P_2P_1^\mathrm{T}$ 修正为真题原貌的逆矩阵版 $P_1^{-1}P_2$ / **$P_2P_1^{-1}$（答案 D）**，解析与思路同步重写（此前"推导对不上选项、待核"的存疑标注已消除）
- `9b6cd44` 2023填空(15) 积分上限勘误：题面误写为 $\int_0^3 f$，真题原为 $\int_1^3 f$（答案 $1/2$）。$\int_0^3$ 因缺条件确实不唯一、才会触发旧"待核/条件不足"标注；改为 $\int_1^3$ 后答案唯一成立，标注清除。
- 背景：大观园题库（854 条数二真题）全量比对未发现其他需改项，报告见 `_verify/compare_*_v2.md`

## 〇、真题配图功能（最新）
- `6b7b513` 从 `mathnote_ocr/` 源页裁切 9 张配图（2003Q4/2005Q16,Q17/2007Q3/2008Q2,Q6/2009Q6×2/2010Q18），存入 `pwa/data/img/exam_fig/`
- exam.json 8 道题加 `img` 字段，exam.js qCard 渲染 `<img class="q-fig-img">`，exam.css 居中自适应
- 6 道题缺源图（2001/2002/2011/2015/2016/2017）待补原始 PDF

## 一、真题公式渲染修复
- `9c235c3` 修复 2000/2001/2003/2004/2006 真题源文件 `$$` 不配对（stray `=$$`、单开双闭等），重建 exam.json，KaTeX 0 失败。

## 二、二次型母题 + 常考题型导航
- `b980695` 二次型母题(II)改为配方换元法，新增 14 项常考题型导航（渲染器支持行内锚点 `- {#id}`）。
- 配套线代6 章节以母题为蓝本重写（求 P 三法：配方/方程/正交；题型一/二 一次/两次转换），见 `8b584ff` / `8ed7003` / `11eeb8f` / `c31935b`。

## 三、搜索联动左侧导航
- `0a26747` 搜索与左侧导航联动跳转。
- `a529789` 改为点击结果后定位导航栏，去掉导航树批量高亮（用户反馈"高亮太多"）。

## 四、真题收藏星标修复
- `3b8baa8` `toggleFav` 只切 class 未换字符 → 同步 ⭐/☆ 字符与 title。

## 五、刷新重定位
- `63c5a75` 笔记页 + 真题页记录并恢复上次浏览位置（savePos/restorePos，记录 `{id,ch,y}`）。

## 六、真题思路 + 笔记功能（重大新增）
- `88f1ff2` 真题每题新增「思路」「笔记」，置于「查看答案」右侧同行（2007 全卷 24 题试水）。
- `44be032` 思路改分行分题号排版；思路区分式字号调小。
- `fcb1ef8` 推广至全部 27 套真题（2000-2026，共 607 题）；`build_exam.py` 支持 `::: idea` 块解析。
- `1148709` 思路区分式恢复默认字号（上轮 0.85em 压太小）。

## 七、查看答案按钮修复
- `43c5ab6` `data-act="ans"` 拼出 `q-ans` 与 div 类 `q-answer` 不符 → 统一为 `answer`，按钮恢复响应。

## 八、真题源数据全量核对（数据质量）
- `4073f0a` 逐个核对修正真题源数据约 30 处（确保数学正确）。
- `8da175e` 用官方答案 PDF（`D:/ai code/数学二真题套卷以及答案`）逐题核对，修正 4 处严重转录错误（如 2000Q1 题面 arctan x - x 误写）。
- `12a951a` 2014-2025 共 12 卷视觉核对：渲染原卷+答案 PNG（263MB），6 个 Agent 并行视觉核对，修正 8 处 md 源数据问题：
  - 2014 Q17 答案 `1/4` → `-3/4`（分部积分符号错）
  - 2015 Q1 补全 D 选项（题面转录漏）
  - 2016 Q18 补全被积函数
  - 2016 Q23 删多余解析并补 A⁹ 正确矩阵
  - 2018 Q3 f/g 分段定义完全对调
  - 2022 Q1 选项修正
  - 2022 Q9 题面右端向量 `b` → `b₀`（与节点 b 区分）
  - 2022 Q20 删 `(1,1)` 极大值（实为鞍点）
  - 注：2023 Q15、2025 Q22 为 Agent 误报，保持原样
  - KaTeX 校验：14647 公式 0 失败

## 九、关键文件清单
- 源数据（仓库外）：`D:/ai code/数学二真题/*.md`（2000-2026 共 27 套，含 `::: answer` / `::: idea` 结构）
- 构建产物（入库）：`pwa/data/exam.json`
- 渲染逻辑：`pwa/js/exam.js`（qCard/toggleQSec/toggleFav/jumpToQ/位置记忆）、`pwa/js/reader.js`（搜索联动/行内锚点/重定位）
- 样式：`pwa/css/exam.css`（`.q-sec.q-idea` 蓝色左边框 + 分段距 7px）
- 构建工具：`tools/build_exam.py`（支持 `::: idea` 块 + 构建期 `$$` 告警）、`tools/_katex_check.js`（覆盖 idea 字段校验）

## 十、部署注意
- 线上需 Ctrl+F5 硬刷新加载新 SW/CACHE_VER（pre-commit 钩子自动 bump）。
- 真题 md 源在仓库外，git 仅提交构建产物 exam.json + 工具。

## 十一、真题配图 / 笔记 单击放大图片（最新）
- `pwa/js/exam.js` 新增 `zoomAnsImg(img)`：懒创建全屏遮罩 `#zoomOverlay`（复用 `exam.css` 的 `.zoom-overlay`），点图放大、点遮罩空白/Esc 关闭；放大图本体 `stopPropagation` 避免误关。
- `qCard` 的真题配图 `.q-fig-img`（含 `img2`）加 `onclick="zoomAnsImg(this)"`；
- 笔记贴图渲染 `mdBlockWithImg` 的 `.exam-note-img` 加 `onclick="zoomAnsImg(this)"`（笔记预览与持久渲染两条路径都经此函数，统一生效）。
- `pwa/css/exam.css` 给 `.q-fig-img` 与 `.exam-note-img` 加 `cursor: zoom-in` 提示可点。
- 与 `selected_ans.js` 的 `zoomAnsImg`/`.zoom-overlay` 同一套遮罩，行为一致；选填整卷答案图此前已支持，本次补齐真题页配图与笔记贴图。
- 提交触发 pre-commit 钩子重建 `pwa/sw.js` 的 `CACHE_VER`，线上 Ctrl+Shift+R 强刷即可见。

## 十二、顶部栏（header）整体向上折叠（最终版）
- 需求：整个顶部 header（标题+按钮）整行折叠消失，箭头常驻右上角悬浮、收起时独立显示。
- `pwa/exam.html`：`#examTop` 为「`.exam-brand` 标题 + `.exam-topbtns` 三按钮」；箭头 `#examTopToggle`（▲）`onclick="toggleTopBar()"` 是 body 直属节点（header 隐藏后仍在）。
- `pwa/css/exam.css`：`.exam-top-toggle` = `position:fixed; top:12px; right:14px; display:flex` 常驻右上角（展开/收起都可见，满足"箭头一直保存右边"）；`.exam-top` 加 `padding-right:56px` 给箭头留出空间避免遮挡按钮；`.exam-top.collapsed` 整栏 `display:none` 完全消失；`.exam-top-toggle.collapsed` 时箭头 `rotate(180deg)` 朝下。
- `pwa/js/exam.js`：`toggleTopBar(force)` 同时切 `#examTop` 与 `#examTopToggle` 的 `.collapsed` 类 + `localStorage` 持久化 `examTopCollapsed`；`restoreTopBar()`（`init()` 末尾调用）恢复历史态。
- 修复记录：曾误把展开态箭头 `display:none` 隐藏（与"箭头一直右边"冲突）；改为常驻显示，并用 padding-right 避让按钮，消除遮挡 bug。

## 十三、题库（大观园）全量交叉比对 真题（2026-08-23~24）
- 用 `D:/cjx/下载/Compressed/daguanyuan-for-windows-main.1.1/.../assets/questions.json`（5952 题）筛出 **数二真题 854 条**（规则：source 含「数二」+ 年份 1987–2026 + 排除练习册标签 880/1000题/660/1800/…），与 `pwa/data/exam.json`（2000–2026，27 套 607 题）做逐题（年,题型,题号）相似度匹配。
- 比对引擎（`tools/_verify/compare_bank.py` 已沉淀为 skill `mathnote-bank-compare`）：选择题比字母；填空/选择经 LaTeX→sympy（`latex2sympy2`）判定不等 → DIFFER（高置信）；解答题 → REVIEW（人工）。
- 结果：14 条 DIFFER + 49 条 REVIEW，逐条核实 **全部为等价改写 / 不同小题 / 题库自身错误**，exam.json 无真实答案错误需改：
  - 2001填5、2009填9：题面矩阵/幂次与题库不同（非同题，exam 正确）。
  - 2006选10：exam (B) 正确，题库写 D（题库错）。
  - 2005填1：exam `−π`（求 y′），题库 `−πdx`（求 dy，题面不同）。
  - 2001填5：exam `−2` 正确（|A|=(a+2)(a−1)²，a=−2 秩2<3 有无穷多解），题库 `a=±√2`（错）。
  - 2009填9：exam `y=3x`（t³），题库 `y=2x`（t²，错版）。
  - 其余 DIFFER（2003填4、2007填15、2008填10、2010填13、2013填9/10/12、2019填13、2023填12、2025填15、2026填11）均为等价异形（如 `1/(4a)(...)`=(...)/(4a)、`Cx−xe⁻ˣ`=x(C−e⁻ˣ)、`√e`=e^{1/2}、(0,2)=0<p<2）。
- **顺手修正 1 处真实笔误**：2010 解答(20) 推导中 `cos 4θ`（应为 `cos⁴θ`），最终值 `1/3−3π/16` 正确不受影响。
  - `abf836d` fix(数据): 2010数二解答(20) cos 4θ 笔误修正为 cos⁴θ（已 push，远端 HEAD=abf836d）。
  - KaTeX 审计 0 失败。
- 三份报告：`tools/_verify/compare_report_v2.md`（全量）、`compare_disagree_v2.md`（14 条高置信分歧）、`compare_review_v2.md`（49 条解答题待核）。
- 注：2009 填(9) 建议后续用真题 PDF 原件终核（题库 y=2x 实为 t² 情形，exam 的 t³→y=3x 数学已确认正确）。

## 十四、笔记双份显示修复 + 有笔记默认展开（最新）
- **Bug 根因**：有笔记的题展开后，textarea 显示原文 + `.q-note-preview` 显示渲染版，同一内容出现两遍（如「光滑曲线：有一阶连续导数」×2）。
- `pwa/js/exam.js`：
  - `qCard` 笔记区分两形态——**有笔记**：默认展开（`.q-note` 不加 hidden），只显示渲染预览，textarea `display:none` 隐藏，附「✏️ 编辑」小按钮；**空笔记**：保持原样（hidden，点「笔记」按钮出输入框）。
  - 新增 `toggleNoteEdit(btn)`：「✏️ 编辑」→ 只显示输入框（按钮变「💾 完成」，隐藏预览）；「💾 完成」→ 清防抖定时器立即落盘 → 渲染预览 + `renderMath(pv)`（KaTeX）+ 贴图回填；若内容被清空则收起整节并重置为空笔记形态。
  - `noteInput` 去掉实时预览（编辑态只显示输入框，杜绝原文+渲染双份）。
  - `renderCurrent` 后对 `.q-note-preview:not([hidden])` 批量 `fillExamNoteImgs` 回填 `[图:id]` 贴图。
- `pwa/css/exam.css`：新增 `.q-note-editbtn` 小按钮样式。

## 十五、整体布局审查：顶部栏高度联动修复（最新）
- **缺漏**：`.exam-wrap`/`.exam-side` 三处硬编码 `53px`（header 高度）——收起顶部栏后侧栏仍从 53px 开始，页面顶部留白、侧栏短一截；窄屏 header 换行变高时也不准。
- 修复：CSS 变量 `--exam-top-h`（默认回退 53px）替换三处硬编码——`exam-wrap.min-height`、`exam-side.max-height`、`exam-side.top`。
- `pwa/js/exam.js` 新增 `syncTopH()`：读 header 实际 `offsetHeight`（收起=0）写入 CSS 变量；在 `toggleTopBar`/`init()` 末尾调用，并用 ResizeObserver（降级 resize 事件）监听 header 尺寸变化自动同步。
- 其余审查结论：悬浮箭头（右上 fixed）与题号导航（右下 fixed）无冲突；zoom 遮罩 z-index 9999 正确覆盖全部浮层；笔记三形态（只读预览/编辑/收起）状态机闭环。

## 十六、真题数据并入笔记备份 JSON（换设备迁移）（最新）
- **缺口**：原「导出标注」JSON 只含笔记站批注+贴图；真题页的笔记文本（`examNote-*`）、收藏（`examFav`）、试卷点评（`examReview-*`）、笔记贴图（IndexedDB `examNoteImg`）都不在包里，换设备即丢。
- `pwa/js/annotate.js`：
  - 新增 `collectExamData()`：扫 localStorage 抽 `examNote-*` / `examReview-*` / `examFav`，从所有真题笔记文本抽 `[图:id]` 回源 IndexedDB 转 base64。
  - 新增 `restoreExamData(exam)`：导入时还原文本/点评（直接写回），收藏**合并**（Object.assign 防覆盖本机已有），贴图写回 `examNoteImg` DB；任一环节失败不阻塞其余。
  - 导出 payload 加 `exam` 字段（`__v` 保持 2，旧站点导入新包时忽略该字段，向后兼容）；导入成功提示注明「含真题笔记/收藏/点评」。
- 使用：换设备 → 笔记站「导出标注」下载 JSON → 新设备笔记站「导入标注」→ 真题页刷新后笔记/收藏/点评/贴图全部恢复。UI 无新增按钮。

## 十七、真题分类浏览（大观园知识点体系）（最新）
- **数据落地**（`a6ed98e`，已 push）：607 道数二真题（2000–2026，27 套）**全部**打上 `categoryIds`（单元素数组，存大观园二级章节 id），12 章体系——高数 6 章（极限146/一元微分120/一元积分66/多元微分52/微分方程52/二重积分25）+ 线代 6 章（矩阵32/线性方程组31/向量29/特征值25/行列式17/二次型12）；新增查名表 `pwa/data/exam_categories.json`（`{id:{id,name,path,parent}}`，仅实际用到的 12 章）。
- **分类口径**：大观园「知识点/章节」体系（非题型）；自动匹配率仅 36% 时改「银行高置信(sim≥0.8)优先 + 关键词规则补全」达成 100% 覆盖，兜底 17 题默认归一元微分（多为证明题）。
- **新页面 `pwa/category.html`**（独立全屏浏览页，参照 `selected.html` 形态）：
  - 左栏二级（学科/章节）可折叠分类树，**只显示真题里实际出现的章节 + 题数**，章节按题数降序（最常考排前）；学科按 高数→线代 固定顺序。
  - 点章节 → 右栏跨年罗列该考点全部真题（卡片复用 `exam.js` 的 `qCard` 渲染：题干/配图/选项/思路/笔记/答案 + 收藏星标 + 单击放大）；年份徽标链接 `exam.html?paper=<套卷id>` 可下钻到真题页对应套卷。
  - 顶部「⭐ 收藏夹」开关：**开启后分类树与题目列表都只限收藏题**（共享 `examFavOnly` key，与真题页联动）。
  - 题型标签由 section 标题判定（选择/填空/解答），规避老卷 no 区间启发式误判（2001 填空=1–5、选择=6–10）。
- `pwa/exam.html` header：**「⭐ 收藏夹」左侧新增「🗂 分类」入口**跳 `category.html`（仅此题加入口，其余页不动）。
- `pwa/js/exam.js`：`init()` 支持 `?paper=` 深链，分类页年份徽标点击直达对应套卷。
- `pwa/js/category.js`：自带渲染/收藏辅助副本（与 `exam.js` 同源，避免改动真题页既有逻辑）；`pwa/css/category.css` 新增分类树/年份徽标样式。
- `pwa/sw.js` PRECACHE 加 `category.html` / `css/category.css` / `js/category.js`，提交触发 pre-commit 钩子自动 bump `CACHE_VER`。
- **自测**：`node --check` 双 JS 通过；数据完整性 607/607 全分类且 id 合法；聚合逻辑经 Python 复刻 + Node DOM 桩跑通真实 `init()`（全量树正确、favOnly 仅留收藏章节/题数、下钻链接生成）；`tools/_audit_latex.py` 0 失败。

## 十八、真题分类升级为三级（大观园 学科/章节/知识点）（最新）
- **需求**：用户要求「按大观园的三级分类」。先复述需求+指出模糊点请确认（Plan 模式），确认项：① 每题单一 L3 路径；② 混合策略（题库高置信+关键词+章内默认叶）；③ 标签清洗后显示（去 LaTeX/截断）；④ 仅三级可折叠（停在 L3，不展开 L4+）。
- **数据重生成**（`_verify/classify_exam.py` 重写为 v3）：607 题 `categoryIds` 改为存 **L3 知识点叶子 id**（59 个，12 章各 2–10 叶），L1/L2 由 `parentId` 链推出；`exam_categories.json` 升级为 **73 节点**（2 学科 + 12 章节 + 59 知识点），每节点含 `display`（清洗名，去 `$...$`/空格/截断 22 字）。
- **分类策略（混合，章作用域防跨章污染）**：
  - ① 题库（`questions.json` 5952 题）逐题相似匹配 `sim≥0.8` 高置信 → 取其 `categoryIds` 沿 `parentId` 上卷到 L3（命中 103 题）。
  - ② 否则 `CHAPTER_RULES` 定章 → 章内 `LEAF_BY_CH` 关键词细分到 59 叶之一（命中 308 题）。**关键修正**：v2 全局 L3 关键词导致 `f(x)` 等跨章污染（行列式/多项式误吞 109 题），改为「先定章、章内再分叶」后消除；并把 `一元微分` 章提到 `极限` 前、给其加中值定理关键词，使 3 道中值定理题正确归入「一元微分/微分中值定理」。
  - ③ 章内未细分 → 落该章默认叶（181 题，详见各章兜底统计）；④ 仍无章 → 全局默认叶「导数计算」（16 题）。
- **三级分布（合计 607）**：极限/极限86 · 一元微分/导数计算71 · 极限/连续32 · 一元微分/导数应用53 · 导数概念17 · 多元微分/偏导48 · 微分方程/解微分方程37 · 以形式给出13 · 一元积分/计算33 · 应用31 · 反常5 · 二重积分/计算13 · 其他12 · 线性方程组/求解24 · 矩阵/其他16 · 逆9 · 向量/相关14 · 表示9 · 特征值/对角化7 · 实对称7 · 相似6 · 特征向量5 · 行列式/抽象13 · 二次型/标准型4 · 惯性4 等。少数可识别题型叶子为 0（中值定理已修；高次幂/正定/重极限/合同等少数题落章默认叶，准确率中等，可后续人工微调）。
- **UI（`pwa/js/category.js` + `pwa/css/category.css`）**：分类树由二级改为**三级可折叠**（学科 ▸ 章节 ▾ 知识点），每层显示题数并随 `favOnly` 过滤；点知识点叶子 → 右栏罗列该考点跨年真题（沿用 `qCard`/`年份下钻`/`收藏`）；`cleanLabel` 清洗树标签（hover 显示原 path）；主区标题加 `path` 面包屑。`category.html` 副标题改为「按大观园三级分类（学科/章节/知识点）」。
- **自测**：`node --check` 双 JS 通过；607/607 id 全合法；`_verify/smoke_category.js` 用真实数据 + DOM 桩跑通真实 `init()`（三级树渲染、favOnly 仅留 2 收藏叶子/合计=2、下钻链接生成）SMOKE PASS；`_audit_latex.py` 0 失败；静态服务 8 文件全 200。
- `exam.html` 入口与 `sw.js` PRECACHE 沿用十七（无需改动）。
- **十八-2 一键展开答案按钮**（增量）：`category.js` 主区标题栏（`paper-head`）加「🔼 展开全部答案 / 🔽 收起全部答案」切换按钮，作用于当前右栏所有题卡——一次性把 `.q-answer` 的 `hidden` 置否，并同步每张卡「查看答案」按钮的 `on` 态与文案；重选分类后默认回到折叠态。新增 `toggleAllAnswers()`；`category.css` 加 `.all-ans-btn` 样式（accent 描边胶囊按钮，hover/on 态）。`_verify/smoke_allans.js` 用 DOM 桩验证展开/收起逻辑 PASS。
- **十八-3 记录收藏时间**：`exam.js` / `category.js` 的 `examFav` 存储由 `{qid:1}` 升级为 `{qid:{t:时间戳}}`，`isFav` 用 `!!favGet()[qid]` 天然兼容旧 `1`；新增 `favTime(qid)` / `fmtFavTime(ts)` 辅助。两页 `toggleFav` 收藏时写 `{t:Date.now()}`；卡片收藏按钮 `title` 显示「收藏于 yyyy-MM-DD HH:mm」（旧数据无时间显示「已收藏」）。`category.js` 收藏过滤（`favOnly`）按收藏时间倒序（最近收藏排前），真题页单卷内保持原顺序。`_verify/smoke_favtime.js` 验证旧格式兼容 + 新格式写入/取消 ALL PASS。
- **十八-4 星标左侧显示收藏日期徽标**：已收藏的题在星标（⭐/☆）左边显示「MM-DD」日期徽标（`.q-fav-date`，hover title 显示完整「收藏于 yyyy-MM-DD HH:mm」）；旧格式 `{qid:1}` 无时间不显示徽标。两页 `qCard` 模板同步注入；`toggleFav` 收藏/取消时**就地增删徽标**（免整页重渲染，favOnly 模式仍走重渲染）。CSS：`.q-fav-date` 接管 `margin-left:auto` 靠右、`.q-fav-date + .q-fav` 归零间距（加在 exam.css，exam/category 两页共用）。`_verify/smoke_favdate.js` 用真实数据 + DOM 桩验证：新格式徽标渲染 08-25、旧格式无徽标不报错、未收藏无徽标 SMOKE PASS。
- **十八-5 旧收藏一次性迁移为今天**（十八-4 后续）：用户要求把升级前收藏的旧格式题补上日期。两页脚本加载即执行 `migrateFavTimes()`——遍历 `examFav`，凡非对象条目（旧 `{qid:1}`）就地改写 `{t:Date.now()}` 并 `favSave` 写回 localStorage，**真实嵌入存储而非仅显示**；已是新格式的条目时间戳不动。`_verify/smoke_favmigrate.js` 验证：旧格式迁移为今天 ✓、新格式保持原时间戳 ✓、已写回存储 ✓ SMOKE PASS。

## 十九、分类页悬浮抽屉 + 独立收藏夹视图（不分类）+ 不开新窗口（最新）
- **需求**（grill-me 反复确认）：① 分类目录太长 → 改成悬浮抽屉式（参考截图，但图片端点不支持，按文字描述实现）；② 真题分类界面单独搞一个**不分类**的收藏夹界面；③ 不开新窗口（撤销此前给 exam.html 分类入口加的 `target="_blank"`）。
- **悬浮抽屉（`pwa/category.html` + `pwa/css/category.css` + `pwa/js/category.js`）**：
  - 顶部分类入口改为**左下角悬浮 📂 圆形按钮**（`#catDrawerTrigger`）；点开后左侧滑出抽屉 `#catDrawer`（含遮罩 `#catDrawerOverlay`，点遮罩/✕ 关闭），主内容区 `#catMain` 占满宽度（`cat-main-full`）。
  - `category.js` 新增 `toggleCatDrawer()` / `closeCatDrawer()`；选中知识点 `selectCat(id)` 在渲染后调用 `closeCatDrawer()` 自动收起抽屉。
- **独立收藏夹视图（不分类）**：
  - 顶部「⭐ 收藏夹」按钮由 **favOnly 过滤模式** 改为 **viewMode 切换**（`'cat' | 'fav'`），`toggleFavView()` 切换后在主区渲染 `renderFavView()`——**按收藏时间倒序、不分章节**列出所有已收藏真题（含 practice 练习），每张卡片用 `.q-cat-tag` 标注所属考点（取 `categoryIds[0]` 的 display）。
  - 移除原 `favOnly` / `FAV_ONLY_KEY` 持久化逻辑；`selectCat` 切回分类视图时重置 `viewMode='cat'` 并取消收藏夹按钮高亮；`toggleFav` 在收藏夹视图下实时 `renderMain()` 使取消收藏即时移出。
  - 空收藏显示引导文案；初始主区提示改为「点击下方 📂 按钮展开分类抽屉…」。
- **不开新窗口**：`exam.html` 顶部「🗂 分类」与底部导航、以及 `category.html`「📚 笔记」的 `target="_blank"` 全部移除，改为同窗口跳转（原 year 下钻链接 exam.html?paper= 本就同窗口，保持不变）。
- **自测**：`node --check pwa/js/category.js` 通过；全文检索确认无遗留 `favOnly` 变量 / `toggleFavOnly` 引用（仅剩按钮 id `favOnly`）、无 `target="_blank"` 残留在 exam.html / category.html。
- **注意**：`index.html` / `good.html` / `selected.html` 仍保留 `target="_blank"`（笔记站各模块入口，不在本轮分类抽屉任务范围，未改动）。
- 完成后 git commit + push（pre-commit 钩子自动 bump `sw.js` 的 `CACHE_VER`，线上 Ctrl+Shift+R 强刷生效）。

## 二十、KaTeX 渲染修复：展开答案/思路时 $$ 块不渲染（最新）
- **根因**：`renderMath(root)` 仅在 `renderMain` / `renderFavView`（`exam.js` 的 `renderCurrent`）初始渲染时调用一次；答案（`.q-answer`）和思路（`.q-idea`）段落初始 `hidden`，用户点击「查看答案」/「展开全部答案」后 `toggleQSec` / `toggleAllAnswers` 只切换 `hidden`，**从未对新可见内容调用 `renderMath`**，导致 `$$` 块显示为原始文本。
- **修复（`pwa/js/category.js` + `pwa/js/exam.js`，commit `3199434`，已 push）**：
  - `category.js toggleQSec(btn, act)`：展开（`open===true`）后补调 `renderMath(sec)` + `fillExamNoteImgs(sec)`。
  - `category.js toggleAllAnswers(btn)`：批量展开后补调 `renderMath(root)` + `fillExamNoteImgs(root)`。
  - `exam.js toggleQSec(btn, act)`：展开非笔记段落后补调 `renderMath(sec)`。
  - `exam.js examToggleAllAnswers(btn)`：批量展开后补调 `renderMath(el)` + `fillExamNoteImgs(el)`。
- **全量数据审计**：`practice.json` 1402 题（高数+线代，stem/answer/idea 三字段）逐题扫描：`$$` 配对无误（0 odd count），`$` 配对无误，smart quotes 25 处均在数学块外（不破坏 KaTeX），无 Unicode 数学符号混入 math 块。
- CACHE_VER 经 pre-commit 钩子 bump 至 `mathcards-60d9d1eb06`，线上 Ctrl+Shift+R 强刷可见。

## 二十一、分类抽屉改完整多级树（参照大观园层级，最新）
- **背景**：用户 grill-me 要求「按多级格式来分，参照原大观园的格式」。原 `catLink`+`buildTree` 把任意深度（L2~L8）叶子**拍平到 L1 章节下**当一堆并列 `cat-leaf`，丢失了 L2~L8 多级路径——这正是"多级格式"要修的。
- **数据核实**：`exam_categories.json` 共 963 节点，真实深度 L0→L8（最深链长 9，链如「高等数学/极限/极限/极限计算/数列极限/n项数列极限计算/n项和/定积分定义求n项和极限/0到1，n等分，取端点」）；16 个 L0 根里 8 个为概率/数一二三/归档等空分支。`practice.json`+`exam.json` 共 403 个不同 `categoryId` 被使用，分布在 L1~L8，且 52 个被使用的中间节点自身还有子节点（题直接挂在中间层）。
- **实现（`pwa/js/category.js` + `pwa/css/category.css` + `pwa/category.html`，commit `f287f0e`，已 push）**：
  - 废弃 `catLink`/`buildTree`（三级拍平），新增 `buildTreeData()`：`childrenMap` 由 `parentId` 还原整棵树，`directCount` 记直接题数，`subCount` **向上累计**（父节点显示整棵子树总题数）。
  - `renderTree()` 递归 `treeNodeHtml(id, depth)` 逐级渲染；空分支（`subCount===0`）自动隐藏；默认展开到 L1、L2+ 收起，避免初始过长。
  - `toggleNode(id, ev)`：箭头按钮展开/收起（阻止冒泡避免误选中）；节点名点击 `selectCat`。
  - `selectCat(id)`：自动展开所选节点到根的路径（高亮可见），`renderMain` 用 `subIdsOf(id)` 拉取整棵子树题目，并按 `qid` 去重（同一题被多个子分类引用时不重复）。
  - 新增 `.tree-node`/`.tree-arrow`/`.tree-name` 样式；页面副标题改为「按大观园多级分类（学科/章节/考点…）」。
- **算法验证（Node 跑真实数据）**：渲染根仅 高等数学(1632) + 线性代数(404)（数二不含概率统计，空分支自动隐藏，符合只导入数学二）；叶子 `2335`=1 题、L1 `矩阵`=78 题，子树拉取与去重正确；`parentId` 无环无坏引用。
- CACHE_VER bump 至 `mathcards-e800517012`，线上 Ctrl+Shift+R 强刷可见。

## 二十二、真题分类退回「引入大观园之前」（用户要求：只有真题）
- **指令**：用户「把真题分类退回到引进全部大观之前的版本吧 只有真题的那时候」。
- **做法**：`git checkout 338ec28^`（`338ec28`=导入大观园题库那次）恢复 `category.html`/`category.css`/`category.js`/`exam_categories.json` 到只有真题状态——即 `29bb6d1` 三级分类（`91b4ade` 起的固定两栏+`favOnly` 收藏过滤），无练习题、无多级树、无悬浮抽屉。
- **保留的两处改进（不开倒车）**：
  1. `3199434` 的 KaTeX 修复——`toggleQSec`/`toggleAllAnswers` 展开时补调 `renderMath`+`fillExamNoteImgs`，否则会重现「答案不渲染」bug；
  2. 用户「不开新窗口」偏好——去掉恢复版 `index.html` 的 `target="_blank"`，统一同窗口跳转。
- **数据核实（Node）**：恢复后只加载 `exam.json`(607 题)；`exam_categories.json` 退回真题版（L0=2 学科/L1=12 章/L2=59 知识点），真题命中全部 L2 共 45 个知识点，**0 缺失**；旧 `buildTree` 写死三级与真题数据完美匹配。
- **`sw.js` PRECACHE 移除 `data/practice.json`**（不再加载，无需缓存）。
- **未删除 `practice.json` 数据文件**（仅停止加载与缓存；如需彻底清理再说）。
- commit `b26e991`，CACHE_VER→`mathcards-7ecf1b1867`，已 push（远端 HEAD 一致）。

## 二十三、笔记纯文字去嵌套双框 + 真题页一键展开答案按钮补样式
- **用户反馈**：截图显示纯文字笔记「构造函数 然后分开讨论」出现两遍——外层 `.q-sec.q-note`（灰底实线框）+ 内层 `.q-note-preview`（浅底虚线框），同一文字嵌套双框。用户要求：**只放图片时才用这种卡片样式，纯文字不要双框**。
- **根因**：`.q-sec.q-note` 和 `.q-note-preview` 两层容器都有 `background`/`border`/`padding`，无论内容是文字还是图片都渲染为嵌套盒子。
- **修复（`exam.css` + `exam.js` + `category.js`，commit `76602fd`，已 push）**：
  1. **exam.js `qCard()` / category.js `catCard()`**：新增 `hasImg = /\[图:[a-z0-9]+\]/.test(note)` 检测，有 `[图:id]` 时给 `.q-note` 加 `.has-img` 类。
  2. **exam.css 新增规则**：`.q-note:not(.has-img) .q-note-preview { margin-top:0; padding:0; border:none; background:transparent }` —— 纯文字笔记的内层预览区变为透明无框，文字直接融入外层容器；含图片笔记保持现有虚线框卡片式样不变。
  3. **真题页一键展开答案按钮**：`examToggleAllAnswers()` 函数和按钮 HTML 已存在于 `renderCurrent()`（L653），但 `.all-ans-btn` 样式只在 `category.css` 定义、`exam.css` 完全缺失 → 按钮无样式/不可见。现补全到 `exam.css`（圆角描边按钮 + hover/on 态）。
- CACHE_VER → `mathcards-b581a590d1`，已 push（远端 HEAD `76602fd` 一致）。


## 二十四、全面体检：答案勘误 + 批注去重 + 题号修复 + 全量审计（2026-08-29）
- **2013 解答(22)（AC−CA=B）答案重写**：题面 $A=\begin{pmatrix}1&a\1&0\end{pmatrix}$ 与最终答案 $a=-1,b=0$、$C=\begin{pmatrix}k_1+k_2+1&-k_1\k_1&k_2\end{pmatrix}$ 本就对，但解析中间推导是按另一种 $A$ 展开的错版（矩阵展开、方程组、"矛盾——改由行变换解"均错）。sympy 验证后按正确推导重写（比较对应元素 → $b=0$ → $-a=1$），思路块同步重写。
- **批注重复治理（双层）**：
  1. 源 md 层：30 份导图笔记里 33 行连续重复的「📝 批注」行去重（11 篇受影响），重建 notes.json；
  2. 存储层（annotate.js）：`apply()` 渲染时跳过与嵌入批注（📝 批注：…）文本相同的存储批注并**就地自愈清理**；marks 按 gid 去重；`markSelection` 同段重划改为换色（防嵌套重复 mark）；恢复列表过滤已嵌入正文的孤儿（防认领回重复）；导入/备份合并按归一化文本去重（`mergeNotes`）。
- **题号重复修复（跳题按钮失效根因）**：2000（解答 13–22+重复 21 → 11–21）、2002（选择 1–5 → 6–10、解答 3–12 → 11–20）、2003（选择 1–6 → 7–12、解答 4–13 → 13–22）——此前同卷题号重复导致 qid 撞车，跳题/收藏/锚点对重复题号失效。修复后 27 套卷题号全部 1..N 连续唯一。
- **源 md 相对旧构建的回退笔误 2 处**：2002 填空(3) $yy''+y'^2=0$ 被误作 $y''+y'^2=0$（答案 $y=\sqrt{x+1}$，整个答案/思路块按错误方程重写）；2007 解答(19) $y''(x+y'^2)=y'$ 被误作 $y'''(x+y^2)=y'$（题干+思路同步修正）。
- **`build_exam.py` 根治分类丢失**：重建会抹掉分类脚本后处理写入的 `categoryIds`（本次实测复现：分类页"暂无分类数据"）。新增 `preserve_category_ids()`——按套卷id+题目顺序从现有 exam.json 搬回，607 题分类已恢复且全落 L2 集合。
- **KaTeX 修复 2 处**：2003 解答(13) 行尾游离 `$`；2000 填空(5) `=$(E+B)^{-1}=$` 多一个 `$`。新增审计工具 `tools/_audit_all_data.py`（5 数据文件结构+定界符）与 `tools/_katex_check_all.js`（全量真实渲染，throwOnError）。终审：56604 公式 0 失败、0 结构问题。
- **exam.js 清理**：移除跳题遗留 console.log 调试代码；scroll 监听从 document 挪到 window（根滚动事件更可靠）。
- **浏览器实测（IAB 实操）**：真题页（27 卷列表/题号导航/跳题/收藏切换/答案展开/2013·2002 修复渲染 0 katex-error）、笔记站（30 篇树/搜索联动 11 结果）、分类页（三级树 461 题聚合、点「导数计算」列 71 题）、好题页、选填页、整卷答案页——全部通过。

## 二十五、真题笔记交互优化：显式保存按钮 + 保存收起输入框 + 文字提亮（2026-08-29）
- **空笔记（直接点「笔记」按钮）**：原来只有 textarea 纯自动保存、无保存按钮、输入框一直开着。现在模板加「💾 保存」按钮（accent 主色醒目样式）——点击立即落盘并收起输入框：非空转为「有笔记」形态（渲染预览 + ✏️ 编辑淡化按钮 + 笔记入口打 has 标），空则收起整节。
- **有笔记编辑路径**：「✏️ 编辑」→「💾 保存」（文案由「完成」统一为「保存」）→ 保存后输入框收起显示预览（原有行为保留）；清空删除分支现在会恢复「💾 保存」按钮的显示（此前保存过一次后该按钮永久隐藏）。
- **toggleQSec 微调**：展开笔记时只读预览态不再触发聚焦/自动保存提示（仅编辑态聚焦）。
- **笔记文字提亮**：预览 14px→15px、行高 1.7→1.75、显式 var(--text)；输入框 13px→14px/1.7。分类页只读预览共用 exam.css 同步受益。
- 浏览器实测三路径全过：空笔记展开→写→保存→收起；编辑→改→保存→收起；清空→删除→保存按钮恢复。

## 二十六、笔记编辑器富文本化（图片内嵌）+ 自动保存防闪烁（2026-08-29）
- **闪烁根因**：自动保存防抖每次触发都对预览区全量 `innerHTML` 重建 + KaTeX 重渲 + 图片重新回填（blob URL 重挂 → 图片重新解码），500ms 一次连打连闪。且「自动保存中…」提示反复重启动画。
- **防闪烁三件套**：① 防抖 500ms→1500ms；② `renderNotePreview` 按 qid 缓存上次渲染文本，**内容没变直接跳过**（`_noteLastRendered`），含公式才调 renderMath；③ `noteHint` 加 quiet 模式——打字中的「自动保存中…」在提示已显示时不重启动画。
- **图片内嵌编辑器**：笔记输入区由 `<textarea>` 改为 **contenteditable 富编辑器**——`[图:id]` 不再以占位文本出现，粘贴/已有图片直接内嵌显示在输入框里（`noteToEditor` 构建 / `editorToNote` 序列化往返，img 设 `contenteditable=false` 可整体删除，× 按钮保留）。textarea 逻辑保留为回退路径（`noteVal`/`setNoteContent` 统一取值/写入）。
- 细节：富文本粘贴降级纯文本防 DOM 污染；预览图片去边框直接融入笔记框；`delExamNoteImg`/`flushNoteSave`/`saveNoteBtn`/`toggleNoteEdit` 全部走统一接口；单图删除/切页冲刷同步预览渲染缓存。
- 浏览器实测：带图笔记进入编辑（图内嵌+blob 回填+无占位文本+序列化往返一致）、修改保存收起、单图删除（token 与 IndexedDB 同步清理）全过。

## 二十七、分类页/好题页刷新状态保持（2026-08-29）
- **分类页（category.js）**：新增 `catViewState`（localStorage）持久化 ① 选中知识点 `curCat` ② 学科折叠集 ③ 章节折叠集——`selectCat`/`toggleSubject`/`toggleChapter` 时写入，init 时恢复（恢复的 id 不在分类表则清除防悬空）。此前刷新回默认提示页。
- **好题页（good.js）**：新增 `goodCur` 持久化当前专题——`openTopic` 写入，init 恢复（无效 id 回退第一篇）。此前刷新固定回第一篇。
- 浏览器实测：分类页选「导数计算」刷新后选中保持 + 71 题恢复；好题页开「好题19-一元函数积分学」刷新后恢复 ✓。
- 至此全部页面刷新保持审计通过：笔记站（hash+savePos+openGroups+搜索筛选）、真题/选填（套卷+题号+滚动+折叠态）、整卷答案（?pid=）、分类页、好题页。

## 二十八、真题点睛工程启动：管线 + 批次1（2000 卷 21 题）（2026-08-29，grill-me 定稿）
- **方案（grill-me 逐项确认）**：① 607 真题逐题「📌 点睛」（公式/易错/技巧/注意四段），分批交付逐批审（按年份 2000→2026，每批 2-3 套）；② UI=题卡第四按钮+四段展开块（紫色调，与思路/笔记同模式）；③ 点睛按 12 章自动聚合生成「高数-真题点睛」「线代-真题点睛」挂进笔记站 gs/xd（脚本可重建）；④ 考前21记对比每章内容只补缺失（逐章 diff，21记不动不删）；⑤ 缺失知识点我判断+总结文档记清单；⑥ 好题(240题)/选填(496题) 只加点睛功能——共 1343 题。
- **管线**：源 md 新增 `::: 点睛` 块（【公式】【易错】【技巧】【注意】段位标记）→ `build_exam.py` 解析为 `q.tips{gs,yc,jq,zy}` → `exam.js` qCard 渲染「📌 点睛」按钮 + `.q-tips` 四段块（四色标签，KaTeX 全渲染）。空段位丢弃，无点睛的题不出按钮。
- **批次1交付（2000 卷 21/21 题）**：每题四段全覆盖，含专属公式、减法型极限不能等价代换、(E+A)(E+B)=2E 配凑、f'g-fg' 凑商导数、外积型矩阵幂次塌缩、秩相等双条件等。源 md 已插入（tools/_tips_2000.py 可重复执行——倒序插入防行号偏移）。
- **踩坑修复**：① finalize_q 的 `else: q.pop('idea')` 与新 tips 分支错误配对导致全库思路被删——已修正配对，重建后 idea 与 HEAD 0 差异；② 插入脚本顺序插入导致行号偏移、块错位——改为倒序插入。
- 审计：56848 公式 0 渲染失败；stem/idea/answer 与 HEAD 逐字一致；categoryIds 607 完好。浏览器实测 21 按钮全渲染、四段展开、0 katex-error。
- **待办**：批次2=2001；…批次10=2026；真题完成后→笔记聚合脚本→选填(selected_tips.json 侧文件+build_selected 合并)→好题(好题/*.md ::: 点睛)→21记逐章对比补充。

## 二十九、点睛/思路分工规范 + 2000 卷点睛去重重写（2026-08-29）
- **用户反馈**：点睛与思路内容重合——公式段复述思路①的具体展开（arctan 展开、(E+A)(E+B)=2E、ln(1+x) 展开等思路里都有）。
- **写作规范（后续所有批次遵循）**：思路 = 本题怎么解（具体步骤、中间展开）；点睛 = 离开本题仍成立的可迁移知识资产。
  - 公式段：只写**泛化模型/可背结论**（如"减法型结论串 x−arctan x~x³/3、x−sin x~x³/6…"），不复述思路已有的具体展开；无泛化价值则整段省略。
  - 技巧段：通用套路与最优路径（"抽象 f 配 xᵏ 分母：泰勒后整体移项"），不与思路步骤重复。
  - 易错/注意：写思路里没有的坑与边界（天然不重）。
- 2000 卷 21 题点睛按新规范全部重写（_tips_2000.py 支持 --strip 剥离旧块后重插）。idea 与 HEAD 0 差异，56848+ 公式 0 渲染失败。

## 三十、点睛批次2：2001 卷 20/20 题（2026-08-29）
- 2001 卷全部 20 题点睛写入源 md（_tips_2001.py，支持 --strip），按二十九节的分工规范。
- **顺手修两处数据质量**：① Q14 答案块烂尾（"f(x)=ln(某表达式)"）→ 重写为完整隐式解推导（对 (f²+2f)e^f=x 求导验证单调唯一性，答案即隐式方程，无初等显式解）；② Q18 题面 f'''(η) 笔误 → f''(η)（题设仅二阶连续导数，解析本就按 f'' 推），删"若含三阶导则类似推导"的含混注。
- 审计：57040 公式 0 失败；idea 0 差异；stem 仅 Q18 预期修正；categoryIds 607 完好。浏览器 20 按钮全渲染 0 katex-error。
- 累计进度：41/607（2000、2001 卷完成）。下一批：2002。

## 三十一、批次3：2001 Q13 答案补全 + 2002 答案核对修复 + 2002/2003 点睛（2026-08-29）
- **用户指令**：答案必须详细正确逐一核对；接着做 5 年（2002–2006）。
- **2001 Q13（曲率半径）答案重写**：原答案"令 u 可得显式""经计算可得 9"跳步。sympy 验证后重写完整推导：ρ=½(4x+1)^{3/2}，dρ/ds=6√x，d²ρ/ds²=6/√(4x+1)，代入=9(4x+1)−36x=9。
- **2002 Q5 题面笔误（新发现）**：矩阵 A₃₃=−2 应为 **2**（迹=4、det=0 与答案 λ²(λ−4)/特征值 4,0,0 一致；原矩阵特征多项式 λ³−8λ+16 对不上）。答案"4"本身正确。答案块重写为完整第一行展开推导+tr/det 双验证。
- **2002 Q11 答案补全**：原"具体计算略"。补全：dx/dθ=(√3−1)/2=dy/dθ → 斜率恰 1；切线 y=x+(5−3√3)/4，法线 y=−x+(√3−1)/4（sympy 验证）。
- **2002 卷 20/20 点睛** + **2003 卷 22/20→22/22 点睛**（含 Q15 \extstyle 笔误修正）。
- **答案核对策略**：烂尾标记扫描（可得显式/经计算/略/数值验证…）扫 2002–2006：2003–2006 零命中（此前多轮全量核对），2002 两处已修。逐题点睛过程中同步核对答案数值。
- 审计：57462 公式 0 失败；categoryIds 607；浏览器 2002/2003 全按钮渲染。
- **累计进度：63/607（2000/2001/2002/2003 完成）。下一轮：2004–2006（完成 5 年要求）。**

## 三十二、真题笔记字色/高亮工具条 + 关闭拼写检查（2026-08-29）
- **红色波浪线根因**：浏览器拼写检查（contenteditable/textarea 默认开启）。编辑器、点评弹窗 textarea、笔记站批注 textarea 全部加 `spellcheck="false"`。
- **字色/高亮**：笔记编辑器顶部新增工具条——字色 5 色（红橙绿蓝紫）+ 高亮 5 色（黄绿蓝粉橙）+ 🧹清除格式。选中文字后点击即应用（execCommand + styleWithCSS，onmousedown preventDefault 保选区；未选中先提示）。
- **存储格式**：编辑器序列化按文本节点扁平化出令牌 `<c:#hex>…</c>`（字色）/`<h:#hex>…</h>`（高亮，可嵌套于字色内），rgb 自动转 hex；构建器栈式还原为 span/mark。旧纯文本笔记完全兼容。预览/只读渲染在 mdBlock 末尾把转义后的令牌还原为样式（先 h 后 c 保嵌套次序）。
- 工具条显隐随编辑态同步（toggleQSec/toggleNoteEdit/saveNoteBtn 三处）；防抖自动保存照常。
- 浏览器实测：应用字色 → 序列化 `<c:#e03131>…</c>`；字色内再套高亮 → `<c:#e03131><h:#fff3bf>…</h></c>`；预览 span+mark 渲染 ✓。

## 三十三、批次4：2004 卷 23/23 题点睛（2026-08-29）
- 2004 卷全部 23 题点睛写入源 md（_tips_2004.py，可 --strip 重插），按分工规范；答案经扫描无烂尾标记（此前核对轮覆盖）。
- 同轮附带交付：真题笔记拼写检查关闭 + 字色/高亮工具条（见三十二节）。
- 审计：57711 公式 0 失败；categoryIds 607 完好。
- **累计进度：86/607（2000-2004 完成，占用户要求的 5 年 4 席）。下一轮：2005、2006 补齐 5 年。**

## 三十四、批次5+6：2005 卷 23/23 + 2006 卷 23/23 点睛（2026-08-29，5 年目标达成）
- **2005**：23 题点睛全交付。Q17 首版点睛写错了积分对象（紧凑读取截断题面，误以为含 y′）——读完整题面后发现被积函数是 (x²+x)f‴(x)，重写为"三次分部积分"版（7×2+1×2+2×2=20）。
- **2006**：23 题点睛全交付（Q18 斯特森型倒数差、Q23 对称矩阵三条钥匙翻译、Q7 凹函数 dy<Δy 等）。\extstyle 笔误 3 处修正。
- 审计：58208 公式 0 失败；categoryIds 607 完好。
- **累计进度：132/607（2000-2006 完成，用户要求的 5 年 2002-2006 全部达成）**。剩余：2007-2026 共 20 卷 475 题。

## 三十五、批次7：2007 卷 24/24 点睛（2026-08-29）
- 2007 卷全部 24 题点睛（_tips_2007.py，可 --strip 重插）。写点睛前对 7 条存疑点睛逐条与存储答案核对（Q2 间断点归属/Q17 解/Q18 驻点/Q20 二问/Q22 数值/Q23 a 值/Q24 矩阵），修正后定稿。
- 教训：heredoc 内嵌 Python 的反斜杠转义层级易错——点睛脚本一律用 Write 工具整文件写入，不用 bash 替换补丁。
- 审计：58430 公式 0 失败；categoryIds 607。
- **累计：156/607（2000-2007 完成）。待办队列：2008→2026 逐卷点睛（_tips_YYYY.py 模板已定型：读卷→写→插→重建→KaTeX 审计→提交）；全部完成后→笔记聚合脚本→选填 tips→好题 tips→21记逐章对比。**

## 三十六、批次8：2008 卷 23/23 点睛 + Q10 通解符号错误修复（2026-08-29）
- **2008 Q10 数据错误（sympy 抓出）**：微分方程 (y+x²e^{−x})dx−xdy=0 的存储通解 y=x(e^{−x}+C) 代回残差 −2xe^{−x}≠0——原解析在 ∫e^{−x}dx 处丢了负号。已修正为 y=x(C−e^{−x})（答案块+思路块同步），sympy 残差 0。
- 2008 卷 23/23 题点睛（_tips_2008.py，可 --strip）；答案扫描无其他烂尾。
- Q12 拐点点睛修正一版：y″=(10/9)(x+1)/x^{4/3} 分母恒正 → 变号只看 (x+1)；x=0 处 y″ 不存在但不变号，不是拐点。
- 审计：58670 公式 0 失败；categoryIds 607。
- **累计：179/607（2000-2008 完成）。待办：2009→2026 共 18 卷。**

## 三十七、批次9：2009 卷 23/23 点睛（2026-08-29）
- **事故与恢复**：批次9 已做过 2009，本轮误重复。strip 删旧 23 条后插入新写 23 条（针对同卷重写、KaTeX 0 失败、内容合格），旧版可从 git 6034658:tools/_tips_2009.py 追溯。亮点：Q19 平移对称秒杀（u=x−1,v=y−1 交换对称 → ∬(x−y)=0，极坐标硬算三行也能收口）、Q15 剥层比阶（1−cosx 给 x²/2、ln(1+tanx) 给 x²/2 主项，x−tanx 是三阶不参与）、Q7 分块伴随系数 2/3 与 3/2、Q13 x→0⁺ 时 x^{2x}→1 不是 0。
- \extstyle 笔误 1 处修正。
- 审计：58921 公式 0 失败；categoryIds 607。
- **累计：202/607（2000-2009 完成）。待办：2010→2026 共 17 卷。**

## 三十八、批次10：2010 卷 23/23 点睛（2026-08-29）
- 2010 卷全部 23 题点睛（_tips_2010.py，可 --strip）。首版 6 条含糊点睛（Q17/Q18/Q20/Q21/Q22/Q23）逐条读完整答案+sympy 核对后重写定稿（Q23 a=−1 特征值 2,−4,5 已验证）。Q1 的 \extstyle 笔误与 Q5 多余大括号修正。
- 工艺教训（再次确认）：点睛脚本必须 Write 整文件写入；heredoc 内嵌 Python 做批量替换会在反斜杠/制表符转义上炸，禁止。
- 审计：59190 公式 0 失败；categoryIds 607。
- **累计：225/607（2000-2010 完成，11 卷）。待办：2011→2026 共 16 卷 382 题。**

## 三十九、批次11：2011 卷 23/23 点睛（2026-08-29）
- 2011 卷全部 23 题点睛（_tips_2011.py，可 --strip）。亮点：Q5 极小值充分条件的符号翻转（A=f″(0)g(0) 由 g(0)<0 翻转）、Q13 极坐标 r³cos·sin 凑 sin⁵cos、Q14 配方降秩正惯性 2、Q21 双分部边界项靠 f(1,y)=f(x,1)=0 消失。
- Q7 数据本身标注"待核"（选项与推导出入）——点睛按方法论写并如实标注。
- \extstyle 笔误 3 处修正。
- 审计：59416 公式 0 失败；categoryIds 607。
- **累计：248/607（2000-2011 完成）。待办：2012→2026 共 15 卷 359 题。**

## 四十、批次12：2012 卷 23/23 点睛（2026-08-29）
- 2012 卷全部 23 题点睛（_tips_2012.py，可 --strip）。亮点：Q2 单零因式求导（(-1)^{n-1}(n-1)!）、Q4 分段积分比较的"对应点代换"、Q6 对称性砍奇项+面积 π−2、Q12 反转自变量解 y=√x、Q19 两方程相减降阶（f′−3f=−2eˣ 一阶线性秒出 f=eˣ）、Q21 (II) xₙⁿ→0 传递 lim xₙ=½、Q23 AᵀA 正定性=r(A)=n 判据。
- 烂尾扫描零命中；答案此前核对轮已覆盖。
- 审计：59682 公式 0 失败；categoryIds 607。
- **累计：271/607（2000-2012 完成，13 卷）。待办：2013→2026 共 14 卷 336 题。**

## 四十一、批次13：2013 卷 23/23 点睛（2026-08-29）
- 2013 卷全部 23 题点睛（_tips_2013.py，可 --strip）。亮点：Q1 等价链逐环传递（α(x)~x）、Q7 AB=C 且 B 可逆 → 列空间相同 → 列向量组等价、Q13 三个解的差分技巧（y₁−y₃=e³ˣ、y₂−y₃=eˣ → 齐次解）、Q14 Aᵀ=−A* + 半正定筛 |A|=−1、Q15 三因子裂项系数和 7、Q16 柱壳法 Vx=Vy、Q21 完全平方式识别弧长。
- 审计：59941 公式 0 失败；categoryIds 607。
- **累计：294/607（2000-2013 完成，14 卷）。待办：2014→2026 共 13 卷 313 题。**

## 四十二、批次14：2014 卷 23/23 点睛（2026-08-29）
- 2014 卷全部 23 题点睛（_tips_2014.py，可 --strip）。亮点：Q2 振荡衰减斜渐近线（y=x+sin(1/x) 恰有渐近线）、Q8 幂零 AB=O 双向推理、Q10 单点导数正≠邻域单调（f=x+2x²sin(1/x) 反例）、Q14 负惯性指数定 a∈[−2,2]、Q17 轮换对称 −3/4、Q20 调和递推 lim nfₙ=1。
- \extstyle 笔误 1 处修正。
- 审计：60195 公式 0 失败；categoryIds 607。
- **累计：317/607（2000-2014 完成，15 卷）。待办：2015→2026 共 12 卷 290 题。**

## 四十三、批次15：2015 卷 23/23 点睛（2026-08-29）
- 2015 卷全部 23 题点睛（_tips_2015.py，可 --strip）。亮点：Q2 参数型 1^∞ 的分段补值、Q3 α 与 β 的双层条件（可导 vs 导数连续）、Q8 Rayleigh 商读对角、Q10 莱布尼茨三项截断（v=x² 三阶后为零）、Q17 三层还原逐条件定常数、Q20 冷却定律时间差、Q21 切线截距夹逼。
- 审计：60477 公式 0 失败；categoryIds 607。
- **累计：340/607（2000-2015 完成，16 卷）。待办：2016→2026 共 11 卷 267 题。**

## 四十四、批次16：2016 卷 23/23 点睛（2026-08-29）
- 2016 卷全部 23 题点睛（_tips_2016.py，可 --strip）。亮点：Q7 相似矩阵的伴随可逆传递（A*~B* 亦成立，错误项是 C）、Q11 解结构反推方程（差分得齐次解 eˣ）、Q15 cos2x 与 2xsinx 的 x² 项相消必须展到四阶、Q19 降阶公式 u'=Ce^{−∫P dx}/y₁²、Q20 星形线参数体积/表面积。
- \extstyle 笔误 3 处修正。
- 审计：60769 公式 0 失败；categoryIds 607。
- **累计：363/607（2000-2016 完成，17 卷）。待办：2017→2026 共 10 卷 244 题。**

## 四十五、批次17：2017 卷 24/23? → 23/23 点睛（2026-08-30）
- 2017 卷全部 23 题点睛（_tips_2017.py，可 --strip）。亮点：Q8 上三角可对角化秩条件（仅 B 可相似于对角 C）、Q10 参数二阶导分母合并核对、Q15 换元消根号（eˣ→1）、Q16 全参数二阶链式在 x=0 的逐项结算、Q17 分部取 (x²−1) 消边界项、Q19 两级罗尔链、Q22 特解直接从 β 的表示系数读。
- **KaTeX 语法教训**：`\dfrac{x^{2}-1}` 单参数用法非法（缺分母）——已改为 $(x^{2}-1)$ 因子。全库扫描其余 _tips_*.py 无同类问题。
- 审计：60989 公式 0 失败；categoryIds 607。
- **累计：386/607（2000-2017 完成，18 卷）。待办：2018→2026 共 9 卷 221 题。**

## 四十六、批次18：2018 卷 23/23 点睛（2026-08-30）
- 2018 卷全部 23 题点睛（_tips_2018.py，可 --strip）。亮点：Q5 对称区间拆奇偶、Q7 若尔当块相似的秩判据（r(E−A)=2）、Q8 分块秩不变（(A,AB)=A(E,B)）、Q9 拉格朗日中值定理的无穷远形式（可与夹逼互证）、Q13 勾股数 3cm/s、Q21 e^t>1+t 贯穿全题、Q22 平方和型二次型的秩=组合系数阵的秩。
- \extstyle 笔误 14 处修正。
- 审计：61278 公式 0 失败；categoryIds 607。
- **累计：409/607（2000-2018 完成，19 卷）。待办：2009? 2019→2026 共 8 卷 198 题。**

## 四十七、批次19：2019 卷 23/23 点睛 + 2009 点睛脚本错名恢复（2026-08-30）
- **批次19 = 2019 卷 23/23 题点睛完成**（_tips_2019.py，可 --strip）。亮点：Q5 曲率圆夹逼（f″<0 → 曲线夹在切线与曲率圆之间 → f(2)<0）、Q7 伴随矩阵秩三段结论（r(A)≤n−2 → r(A*)=0）、Q8 特征值组合枚举（λ∈{1,−2} 且积 4 → 1,−2,−2 → 规范形 y₁²+y₂²−y₃²）、Q13 交换次序 + sin t² 凑 cos t² 微分、Q18 极坐标 r≤sin²θ + 对称砍半（43√2/120）、Q19 等比拱面积（公比 e^{−π}）。
- **事故复盘**：上轮把 2019 的点睛脚本误存为 _tips_2009.py（文件名笔误），若被运行会把 2019 点睛灌进 2009 卷。本轮发现后：从 git 6034658 恢复批次9 原版 _tips_2009.py（真 2009 点睛），保留 _tips_2019.py，两卷重插+审计通过。2009 md 未受实际污染（上轮 strip/insert 实际作用于 2019 md）。
- 教训：点睛脚本的"文件名/目标路径/内容年份"三处必须一致；交付前 grep 脚本内的年份标题自查。
- \extstyle 笔误若干修正。审计：61552 公式 0 失败；categoryIds 607。
- **累计：432/607（2000-2019 完成，20 卷）。待办：2020→2026 共 7 卷 175 题。**

## 四十八、批次20：2020 卷 23/23 点睛（2026-08-30）
- 2020 卷全部 23 题点睛（_tips_2020.py，可 --strip）。亮点：Q5 分段函数偏导回定义+沿路径证不可微、Q6 积分因子 e^{-x} 化单调传递不等式（f(2)>e⁴f(−2)）、Q7 伴随秩三段+解空间维数 3、Q9 双曲函数互逆（arsinh/cosh）参数二阶导 −√2、Q13 二重根 ODE 的 Γ(2) 积分、Q15 斜渐近线 b=1/(2e)（(1+1/x)^x=e(1−1/2x+…) 的倒数展开）、Q20 罗尔+展开读 f″、Q22 半正定临界 a=−1/2、Q23 "α 不是特征向量"⟺P 可逆。
- \extstyle 笔误 134 处修正（Write 时的坏习惯：写作时不要混入 \extstyle）。
- 审计：61828 公式 0 失败；categoryIds 607。
- **累计：455/607（2000-2020 完成，21 卷）。待办：2021→2026 共 6 卷 152 题。**

## 四十九、批次21：2021 卷 22/22 点睛（2026-08-30）
- 2021 卷全部 22 题点睛（_tips_2021.py，可 --strip）。亮点：Q5 sec x 泰勒（1/2 系数）、Q6 两条曲线条件在 (1,1) 交汇解偏导、Q8 平方项全消（正 1 负 2）、Q10 高斯消元不用换行的三角分解、Q14 交换次序 + sin(x/y) 对 x 积分出 −y·cos(x/y)、Q16 四阶行列式 x³ 项枚举、Q18 (1+x)³ 分母变号的凹凸分段。
- 审计：62073 公式 0 失败；categoryIds 607。
- **累计：477/607（2000-2021 完成，22 卷）。待办：2022→2026 共 5 卷 130 题。**

## 五十、批次22：2022 卷 22/22 点睛（2026-08-30）
- 2022 卷全部 22 题点睛（_tips_2022.py，可 --strip）。亮点：Q1 等价四命题辨析（β=−α 反例）、Q4 卷积型变限积分 H″=f、Q5 双瑕点 p 范围、Q9 Vandermonde 行列式分类、Q11 cot x~1/x 的 1^∞、Q12 隐函数二阶 −31/32、Q13 拆分子+arctan、Q15 换元系数 1/3、Q17 f(e^{x²}) 与 f(1+sinx²) 的导数定义双拼（−2f′(1)）、Q21 中点凸+连续 ⟺ 凸。
- 审计：62323 公式 0 失败；categoryIds 607。
- **累计：499/607（2000-2022 完成，23 卷）。待办：2023→2026 共 4 卷 108 题。**

## 五十一、批次23：2023 卷 22/22 点睛（2026-08-30）
- 2023 卷全部 22 题点睛（_tips_2023.py，可 --strip）。亮点：Q1 lne+u 的增量展开（b=1/e）、Q3 双数列 xₙ=sinxₙ 与 yₙ=yₙ² 的同阶对照、Q4 通解有界的特征根分类、Q6 双曲线 lnlx 极值（lnln2<0 符号翻转）、Q7 无极值+有拐点的 Δ 双条件（1≤a≤2）、Q8 分块伴随的右上块、Q11 g 的二阶系数 3/2（e^{x²}−cosx）、Q12 弧长的几何替代（扇形+三角形）、Q14 法线斜率 −9/11、Q15 条件不足的解析补注（如实保留）、Q17 切线截距建方程 y=x(2−lnx)、Q22 从乘法定义直读矩阵。
- 审计：62591 公式 0 失败；categoryIds 607。
- **累计：521/607（2000-2023 完成，24 卷）。待办：2024→2026 共 3 卷 86 题。**

## 五十二、批次24：2024 卷 22/22 点睛（2026-08-30）
- 2024 卷全部 22 题点睛（_tips_2024.py，可 --strip）。亮点：Q1 指数幂间断点左右分裂（第一类可去+第二类）、Q4 sinh 复合对发散序列保持发散、Q5 可微+偏导为 0 的 r 型验证、Q11 竖直切线参数化曲率圆 (x−½)²+y²=¼、Q13 换元 u=x+y 化可分离、Q14 莱布尼茨三项 31e、Q17 广义换元雅可比 1/(2v)、Q18 欧拉方程 x=e^t、Q21 端点线性插值差函数 φ 的两级罗尔。
- 审计：62884 公式 0 失败；categoryIds 607。
- **累计：543/607（2000-2024 完成，25 卷）。待办：2025→2026 共 2 卷 64 题（真题收官在望）。**

## 五十三、批次25：2025 卷 22/22 点睛（2026-08-30）
- 2025 卷全部 22 题点睛（_tips_2025.py，可 --strip）。亮点：Q3 任意解积分收敛 ⟺ 特征根实部全负（含纯虚排除）、Q5 全微分换序（y=4−x² 抛物线）、Q6 万有引力分量积分（r³ 分母消雅可比）、Q7 |f(x)|−f(0) 导数定义的三情形分类、Q11 部分分式 ln((2+a)/2)、Q13 黎曼和 −1/4（x lnx）、Q14 变限积分含 y+t² 的隐函数链、Q20 双圆交集叶形分两段极坐标（12π−112/3）。
- 审计：63131 公式 0 失败；categoryIds 607。
- **累计：477/607+22=499? 543+22=565/607（2000-2025 完成，26 卷）。待办：2026 一卷 42 题（收官）。**

## 五十四、批次26（收官）：2026 卷 22/22 点睛——真题点睛 607/607 全部完成（2026-08-30）
- 2026 卷 22 题点睛（_tips_2026.py）。插入脚本正则适配 `### 1`（无句点）标题格式。
- **里程碑：全部 27 套卷 607 题点睛完成**（2000-2026）。全库审计：63328 公式 0 渲染失败；categoryIds 607 完好。
- 本轮亮点：Q2 可导但导数不连续的 α 区间（1<α≤2）、Q4 混合偏导不可交换经典反例（f_{xy}≠f_{yx}）、Q11 双端判敛 0<p<2、Q12 分子二阶主项 ½、Q17 θ 与 r 完全分离的极坐标、Q19 鞍点与极大判别（8/e²）、Q21 伯努利型缺 y 方程、Q22 分块幂对角化。
- **真题点睛四阶段已达成第 1 阶段（607 题全覆盖）。后续待办：① 笔记聚合（生成「高数-真题点睛」「线代-真题点睛」两篇挂进笔记站）② 选填 496 题点睛 ③ 好题 240 题点睛 ④ 21记逐章对比补充。**

## 五十五、阶段2A：真题点睛聚合笔记（2026-08-30）
- **build_tips_notes.py**：从 exam.json 的 tips + exam_categories.json 的章映射，按"题目.categoryIds[0]=L2叶子 → 叶子.parentId=章节 → 章.parentId=学科根"归属，生成「高数20-真题点睛.md」（461 题/6 章：一元微分144、极限119、一元积分69、多元微分52、微分方程52、二重积分25）、「线代7-真题点睛.md」（146 题/6 章：矩阵32、线性方程组31、向量29、特征值25、行列式17、二次型12）写入 导图/ 目录。
- 每题一行：「📌 年份 题号：公式/易错/技巧/注意四段」，章按题数降序、章首注明题数。
- build_notes.py 重建后挂进笔记站（32 份笔记，高数/线代模块各+1）。可重复执行（脚本再跑即重新生成），不污染手写笔记源文件。
- 浏览器验证：h1/h2/461 列表渲染正常、KaTeX 0 错误、导航树两篇可见。
- 全库审计：70032 公式 0 渲染失败；categoryIds 607 完好。
- **阶段进度：真题目录导航 → ✅ 聚合笔记完成。待办：② 选填 496 题点睛 ③ 好题 240 题点睛 ④ 21记逐章对比补充。**

## 五十六、阶段2C：考前21记逐章对比补充（2026-08-30）
- **对比方法**：提取 21记 16 篇约 150 个章节主题 → 在高数/线代主笔记**全文**检索对应主题词。结论：主笔记对 21记 绝大多数主题已覆盖（未定式/间断点/渐近线/秩1矩阵专题/伴随/正交矩阵等全部命中——先前章节名匹配置的"缺失"多为误报）。
- **真缺失 2 处已补**（Edit 工具改源 md，source 标注 21记）：
  1. **高数1-极限**「函数极限」章 → 新增「左右开弓法（需要分左右极限的情况）」清单：$e$ 型/$arctan(1/x)$ 型/幂指型/取整/分段/绝对值零点——提炼自 21记1。
  2. **线代2-矩阵**「正交矩阵与矩阵等价」章后 → 新增「对照记忆公式」小节：$*,T,-1$ 三符号可交换、乘法分配（顺序反）、加法不分配（$A+B$ 三项）、派生公式——提炼自 21记12。
- 重建 notes.json：全库 70063 公式 0 渲染失败；两处补充均命中。
- **21记原模块未改动未删除**（按需求）。
- 阶段进度：真题 607 ✓ / 聚合笔记 ✓ / **21记对比补充 ✓**。待办：② 选填 496 点睛 ③ 好题 240 点睛。
