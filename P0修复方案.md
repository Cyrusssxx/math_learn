# math-note P0 修复方案（可执行设计文档，不含代码改动）

> 适用范围：考研数学笔记 PWA（`D:\ai code\math-note`）
> 文档性质：**方案设计**，本文件不改动任何代码；按文末「实施顺序」逐步落地时才动代码。
> 配套上下文：见 `缺点评估报告.md`、`缺点评估与改进清单.md`（两份既有报告的核心判断已逐条在源码核实，本方案是其中 P0 两项的可执行化）。

---

## 0. 为什么现在就要做（定位风险，不是救火）

用户当前使用**线上 GitHub Pages 端**，标注一直都在——这是因为：标注存在**访问该站的那个浏览器**的 `localStorage`（键 `notesAnnot`），只要「不改被标注行的原文 / 不清缓存 / 不换浏览器设备 / 不超 5MB」，就稳定。

两个 P0 的失效都是**静默**的，触发条件恰好是产品的正常用法：

- **P0-1（锚点脆弱）**：批注锚点 = `hash(块内文本)`。你回头修订笔记（润色、加标点、补内容）并重新 build 部署后，那行哈希一变，`apply()` 在 DOM 里找不到旧 key → 标注**不再显示**（数据还在 localStorage 里成孤儿，永不回收、永不提示）。你越认真维护笔记，丢得越多。
- **P0-2（配额静默失败）**：`save()` 直接 `setItem` 无 `try/catch`。标注/贴图累积到 ≈5MB 上限后，新标注保存抛 `QuotaExceededError` 被吞掉，**点了保存以为存了其实没存**，毫无提示。

> 结论：当前线上数据**没有丢失**，本方案是预防性加固，让「改笔记」和「标注变多」不再能悄悄吞掉数据。

---

## 1. P0-1：批注锚点从「文本哈希」改为「位置稳定的块 ID」

### 1.1 目标
- 标注绑定到**块在文档中的位置 ID**，而非块内文字。改文字（错字、加标点、补内容）**不影响**已有标注；只有结构性移动/删除块才会失效（且失效时给提示，而非静默）。
- 兼容并**自动迁移**现有文本哈希标注，不丢历史数据。

### 1.2 设计要点
- 在 `reader.js` 渲染期给每个可标注块（`li` / `p` / `td` / 独立 `h2` 不标，保持现状）分配一个**每笔记内唯一、按文档顺序的序号** `sid`，写入元素 `data-sid`。
  - 形如：`data-sid="<noteId>#<seq>"`（`seq` 为该笔记内从 0 开始的块计数；因为 `apply()` 本来就按 `noteId` 分桶，块内只需 `seq` 即可唯一）。
  - 在 `mdToHtml()` / `emitList()` / 段落分支里统一维护一个 `seq` 计数器，每产出一个块元素就 `+1` 并写入属性。**不改动 `notes.json` 数据格式、不动 `build_notes.py`**——sid 在渲染期现算，零数据迁移成本。
- `annotate.js` 改为读 `data-sid` 而非 `data-ak`：
  - `keyBlocks(article)`：把 `el.dataset.ak = hash(...)` 改为 `el.dataset.sid = '<noteId>#' + (seq++)`（或直接在 reader 渲染时写好，annotate 只读取，不再自己算 key——更省事、无双重来源）。
  - `apply(noteId)`：`byKey` 改为以 `data-sid` 建立（`article.querySelectorAll('[data-sid]')`）。
  - 块内高亮（`marks`）维持现有逻辑：块用 sid 定位，块内仍用 `wrapText(block, t, c, n, ...)` 按选中文本 `t` + 第 `n` 次出现定位——这部分与块锚点解耦，无需改。

### 1.3 数据结构变化
| 项 | 现状 | 目标 |
|---|---|---|
| 块锚点 key | `hash(ownText(block))`（`#n` 去重） | `<noteId>#<seq>`（天然唯一，无需 `#n`） |
| 存储结构 | `data[noteId].{hl, notes, marks}` 不变 | **不变**（只有 key 的*取值*从哈希变成 sid） |
| 迁移 | — | 见 1.4 |

> `marks` 记录仍含 `t`（选中文本）、`n`（出现次序）——保留，用于块内重定位，与块锚点无关。

### 1.4 存量标注迁移 + 孤儿回收（关键，保证「不丢历史」）
首次以新版本渲染某篇笔记时（`apply` 内）：

1. 建立 `sid → el` 映射（新规则）。
2. 遍历该笔记已存标注，对**每个旧 key（哈希）**：
   - 若旧 key 能直接匹配某个 `sid` → 已是新格式，跳过。
   - 否则，在本文档所有块里找 `hash(ownText(el)) === 旧key` 的块；找到则把该条标注的 key **改写为那个块的 sid**（自动重锚）。
   - 找不到 → 标记为**孤儿**（旧文本对应的块已不存在/被合并）。
3. 重锚完成后，若有孤儿：在侧栏低调提示「有 N 条标注因内容大改已无法定位，可在「导出标注」备份后清理」，并提供一键清理按钮（清空这些孤儿 key）。**不再静默丢弃**。

> 这样：用户过去在线上积累的标注，升级后**自动续上**；只有确实被整段删除/大改的行才进孤儿流程并被告知。

### 1.5 边界情况
- **嵌套列表**：`emitList` 递归产出的每个 `li` 都独立计数 +1，sid 唯一，父/子块互不干扰。
- **`::: fold` 折叠块**：内部 `li/p` 同样计数，展开后 `apply` 已能定位（现有 `locateBlock` 会展开 `details`），不受影响。
- **整块着色 / 块内高亮并存**：sid 只管「哪一块」，着色与高亮按各自字段挂在同一 sid 下，互不冲突。
- **相同文本的多块**：旧方案靠 `#n` 次序去重，新方案靠唯一 `seq`，更稳，不随段落增删漂移。

### 1.6 验证（落地后）
- 选一篇有标注的笔记，改其中一行的错字/加标点 → 重新 build 部署 → 原标注**仍在**（核心验收点）。
- 删除某块 → 该块标注进孤儿提示，其余正常。
- 移动某块到别处 → 该块标注失效并提示（预期行为，已告知）。
- 导入旧版备份（`__v:2` 文本哈希格式）→ 能被重锚逻辑识别并迁移。

---

## 2. P0-2：`save()` 加配额异常捕获与用户提示

### 2.1 目标
任何写入失败（典型 `QuotaExceededError`）都**立刻告知用户**，不再静默；并给出可操作出口（导出 / 清理）。

### 2.2 改动清单
**`annotate.js`（主）**
- 替换 `const save = () => localStorage.setItem(LS, JSON.stringify(data));` 为带 `try/catch` 的版本：
  - 成功 → 原样。
  - 捕获到配额/写入异常 → 调用一个 `quotaWarn()`：在侧栏底部或顶部弹出非阻塞横幅「⚠️ 标注存储空间已满，新标注可能未保存。请点「导出标注」备份，或清理部分荧光/贴图后重试。」（横幅可手动关闭，不阻断阅读）。
  - 导出动作本身走 `Blob` + `a.download`，**不**走 localStorage，不受配额影响——确保「救数据」通道永远可用。

**`exam.js`（同病，一并修）**
- `favSave(obj)`（L33）同样直接 `setItem` 无保护 → 加同样的 `try/catch` + 轻提示（收藏满时告知）。

### 2.3 可选增强（非 P0，列入后续）
- 将标注整体从 localStorage 迁到 **IndexedDB**（贴图已在 IndexedDB `notesAnnotImg`），从根上解除 5MB 天花板；工作量较大，作为 P0 之后的优化项，不在本方案强制。
- 在侧栏显示「标注占用 / 配额」的粗略进度，给用户提前量。

### 2.4 验证
- 用 DevTools 把站点存储配额临时调小（或构造超大标注）触发 `QuotaExceededError` → 确认弹出横幅、且「导出标注」仍能生成文件。
- 正常保存标注 → 无横幅、体验不变。

---

## 3. 实施顺序与回归检查

| 步骤 | 内容 | 风险 | 备注 |
|---|---|---|---|
| 1 | P0-2（save 配额保护） | 低 | 独立、互不影响，先做能立刻消除「静默丢新数据」 |
| 2 | P0-1 渲染期写 `data-sid` | 中 | 只加属性，不改现有锚点逻辑并行运行 |
| 3 | P0-1 `annotate.js` 切换读 `data-sid` + 迁移/孤儿逻辑 | 中 | 含自动重锚，保证存量标注不丢 |
| 4 | `exam.js` favSave 配额保护 | 低 | 顺手修 |
| 5 | bump `CACHE_VER`（sw.js，现 `mathcards-v25`→+1）| 低 | 必须，否则用户拿到旧缓存 |

**回归检查（每步后）**：
- 荧光（选中/整块）、📝 批注（写/改/删/贴图）、导出/导入合并，逐项点一遍。
- 搜索精确定位（`locateBlock` 闪烁）仍正常——它依赖 `textContent` 找块，与 sid 改动正交，但需确认未误伤。
- 夜间模式、阅读计时、PWA 离线（断网后刷新可用）无回归。

**回滚**：P0-1/2 均为纯前端逻辑，旧 `notesAnnot` 数据格式向后兼容（sid 是新 key，旧哈希 key 仍被迁移逻辑识别）；若回滚代码，旧标注 key 虽暂时无法匹配新 sid，但数据未删，重上新版即恢复。建议回滚前先「导出标注」留底。

---

## 4. 不在本方案范围（避免 scope 膨胀）
- 搜索防抖 / 公式剥离 / 关键词标红（既有报告 2.x，P1）
- `CACHE_VER` 自动 bump（既有报告 4.2，P1）
- 真题模块文档补全 + console 清理（既有发现，P2）
- md 子集补 `###`/有序列表/链接、img src 转义引号（P3）

---

*本方案对应「不动代码」的规划阶段；执行落地时建议从 P0-2 起、按第 3 节顺序推进，并严格遵守 bump `CACHE_VER` 的铁律。*
