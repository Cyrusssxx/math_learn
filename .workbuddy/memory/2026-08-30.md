# 2026-08-30 工作日志

## 好题点睛批次2（好题9-12，32 题）收尾
- 写入 `tools/_tips_good_02.py`（好题9=1、10=12、11=15、12=4，共 32 题四段 tips）。
- 插入：`tools/insert_tips_good.py tools/_tips_good_02.py --force` → 重建 `tools/build_notes.py` → 门禁 `_verify/good_tips_check.js` ✅。
- 门禁结果：含点睛块文件 12 / tip-fold 84 / q-tip-sec 336 / 全库公式 7444（0 失败）/ 点睛内 887（0 失败）。
- 修复：把点睛里误写入 `$...$` 的带圈数字 ① ② 改为纯文本引用（避免 KaTeX 警告）。
- 好题累计 **84/240**（批次1 好题1-8 的 52 + 批次2 的 32）。
- 已补记 `最近改动总结.md` 五十九（批次1）、六十（批次2）两节。
- 待办：好题13-36 共 156 题按 5 批续写；选填 448 题暂缓；点睛总结册（待办3，按"总结归纳重要的"做凝练版 3 篇聚合笔记）留待好题全部完成后启动。

## 好题点睛批次3（好题13-22，52 题）收尾
- 写入 `tools/_tips_good_03.py`（好题13=12、14=6、15=1、16=2、17=1、18=2、19=12、20=10、21=1、22=5，共 52 题四段 tips）。
- 插入 `insert_tips_good.py _tips_good_03.py --force` → 重建 `build_notes.py` → 门禁 `good_tips_check.js` ✅。
- 门禁：含点睛块文件 22 / tip-fold 136 / q-tip-sec 544（四段各 136）/ 全库公式 8141（0 失败）/ 点睛内 1584（0 失败）；stderr 干净（无带圈数字入 $...$ 警告）。
- 好题累计 **136/240**（批次1 52 + 批次2 32 + 批次3 52）。
- 已补记 `最近改动总结.md` 六十一节。
- 待办：好题23-36 共 104 题按 2 批续写；选填 448 题暂缓；点睛总结册（待办3）留待好题全部完成后启动。

## 关键工程事实（好题点睛链路）
- 好题是整篇 md → `good.json.md` 字段，由 `mdrender.js` 渲染；真题/选填走 JSON `tips` 字段。
- `::: 点睛` 渲染两份独立副本 `mdrender.js` 与 `reader.js` 都要改；`notes.css` 共享 `.q-tip-*` 样式（与 exam.css 同源）。
- 中文正则禁用 `\b`（`/^:::\s*点睛/` 而非 `点睛\b`）。
- KaTeX `renderToString` 只认 `displayMode`，校验脚本传 `{displayMode: item.disp}`。
- 源 md 在 `D:\ai code\math\好题\`（仓库外，不入库）；仓库落地 `good.json` + `tools/_tips_good_*.py`。

## 好题点睛批次4（好题23-29，48 题）收尾
- 写入 `tools/_tips_good_04.py`（好题23=7、24=5、25=8、26=2、27=9、28=9、29=8，共 48 题四段 tips）。
- 插入 `insert_tips_good.py _tips_good_04.py --force` → 重建 `build_notes.py` → 门禁 `good_tips_check.js` ✅。
- 门禁：含点睛块文件 29 / tip-fold 184 / q-tip-sec 736（四段各 184）/ 全库公式 9212（0 失败）/ 点睛内 2655（0 失败）；stderr 干净。
- 好题累计 **184/240**（批次1 52 + 批次2 32 + 批次3 52 + 批次4 48）。
- 待办：好题30-36 共 56 题（批次5 收尾）；选填 448 题暂缓；点睛总结册（待办3，按"总结归纳重要的"做凝练版 3 篇聚合笔记）留待好题全部完成后启动。

## 好题点睛批次5（好题30-36，56 题）——好题 240/240 全完成
- 写入 `tools/_tips_good_05.py`（好题30=17、31=3、32=3、33=1、34=5、35=26、36=1，共 56 题四段 tips）。
- 插入 `insert_tips_good.py _tips_good_05.py --force` → 重建 `build_notes.py` → 门禁 `good_tips_check.js` ✅。
- 门禁：**含点睛块文件 36 / tip-fold 240 / q-tip-sec 960（四段各 240）/ 全库公式 10046（0 失败）/ 点睛内 3489（0 失败）/ 无未闭合、无泄漏**；stderr 干净。
- **好题点睛 240/240 全部完成**（批次1 52 + 批次2 32 + 批次3 52 + 批次4 48 + 批次5 56）。
- 已补记 `最近改动总结.md` 六十三节。
- 下一步：启动待办3「点睛总结册」——按用户"总结归纳重要的"做凝练版 3 篇聚合笔记（高数/线代按 12 章提炼，非全量堆砌），挂入笔记站。选填 448 题仍暂缓。

## 待办3·点睛总结册（3 篇凝练聚合笔记）完成
- 生成脚本 `tools/_build_summary_books.py`：exam.json(607)+good.json(240)+selected.json(已做80) 按 12 章→3 篇归类，四段凝练为单行 📌 块。
- 落册（`D:\ai code\math\导图\`，旧真题点睛册保留）：`高数21-高数上·点睛总结册.md`(真题332+好题143+选填51)、`高数22-高数下·点睛总结册.md`(真题129+好题58+选填10)、`线代8-线代·点睛总结册.md`(真题146+好题39+选填19)。合计 真题607/好题240/选填80，缺口416 标待补（待办2）。
- 修 `GOOD_BOOK` 好题8 真实名（`导数定义或其他方法求导`），好题 23 篇全归高数上。
- `build_notes.py` 打包进 notes.json（35 笔记，新册排序 高数20→21→22、线代7→8）；门禁 good_tips_check.js ✅。
- 经验：选填无 categoryIds，按 stem 关键词3分（线代优先→下→默认上）保持各册自洽；源 md 在仓库外不入库，仓库仅落地 notes.json+built。
- 已补记 `最近改动总结.md` 六十四节。选填 416 题（待办2）仍暂缓。

## 批注图片全宽展示修复（CSS）
- 根因：`.exam-note-img-wrap` 为 `inline-block`（收缩包裹，不撑满批注框）；`.exam-note-img` 仅 `max-width:100%` 无 `width:100%`，窄/竖图不放大铺满；`max-height:320px` 进一步压窄。
- 修：`exam.css` wrap→`display:block; width:100%`，img→`width:100%; height:auto`（去限高）。编辑态 contenteditable img 选择器更具体保留自然宽。
- 三页（exam/category/selected）均引 exam.css，批注图一并全宽。

## 线代1「重要行列式」补 11 类例题
- 用户反馈该节无例子。核查 `pwa/data/notes.json` 的 `线代1-行列式` →「重要行列式」节原仅类型/公式口诀、零例题（范德蒙仅一行无步骤紧凑例）。
- 为 11 类各补完整步骤例题（📌 例题 + 解 + 粗体答案），套用原方法口诀；范德蒙追加逐步提因子版。12 道答案全部纯 Python 行列式函数数值验证通过。
- **坑**：改含 LaTeX 的 JSON 用 bash heredoc 会吞反斜杠 → `vmatrix` 行分隔成单 `\`（KaTeX 崩）。改用 Write 工具写脚本（`\\\\`→`\\`）重做，校验 0 坏行分隔、70 处双反斜杠。教训：本仓库改 LaTeX JSON 数据用 Write 工具写脚本，别用 heredoc 直塞反斜杠。
- 已 commit + push（pre-commit bump CACHE_VER），`最近改动总结.md` 新增第六十七节。

## 线代1「重要行列式」补 X形/拉普拉斯/三对角 三类例题（六十八）
- 「重要行列式」节末三类（X形、拉普拉斯主/副对角块、三对角）各补完整步骤例题，共 4 道。
- 答案均纯 Python 行列式函数数值验算通过（X形-18、拉普拉斯主-10、拉普拉斯副-4、三对角32）；KaTeX 双反斜杠校验 0 坏。
- 仍用 Write 工具写插入脚本（避开 bash heredoc 吞反斜杠的老坑），跑完删脚本。
- `最近改动总结.md` 新增第六十八节；commit+push origin/main 完成，pre-commit 钩子自动 bump CACHE_VER。

## 笔记站批注富文本编辑工具条移植（Task #208，阶段二，收尾）
- 用户拍板「要，完整工具条（推荐）」：把真题页 exam.js 的 contenteditable 富文本编辑体系搬进笔记站批注（annotate.js），存量纯文本批注向后兼容。
- 移植函数（annotate.js IIFE 内，与 exam.js 同源）：`ANN_FMT_RE`/`ANN_COLORS`(5字色)/`ANN_HLS`(5高亮) + `annImgNode`/`annEditorToNote`/`annNoteToEditor`/`annHint`/`annApplyFormat`/`annToolbarHtml`；`return` 暴露 `applyFmt`。
- 调用点：`renderNoteBox` 编辑分支 textarea→contenteditable+工具条；`onPasteImg` 改为 Range 插内嵌图；`saveNote`/`cancelNote` 取 `.ann-edit` 经 `annEditorToNote` 序列化（取消按 saved 态清未保存图）。
- 样式（notes.css 对齐 exam.css `.q-note-*`）：`.ann-edit[contenteditable]`/`.ann-toolbar`(满宽)/`.ann-nt-*`/`mark.note-hl`/`.ann-img-wrap`/`.ann-img-del`/`.ann-hint`/`.ann-text h1,h2,b,i`。
- 验证 `node --check annotate.js` ✅；两文件均在 `sw.js` PRECACHE，pre-commit 自动 bump CACHE_VER，commit+push。
- 边界：显示链路 `noteHtml=renderAnnot` 阶段一已含令牌还原；编辑→保存→显示令牌往返一致。`notes.json` 正文无富文本令牌，纯文本批注零迁移。
