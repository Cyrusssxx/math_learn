# 2026-09-20 工作日志

## core_bank 剩余 56 残留字段规范化（自动化任务 6860e9f7）
- 处理 `pwa/data/core_bank.json` 56 个残留 LaTeX 字段（清单 `tools/_todo_core.json`），全部清零。
- **9 批修复脚本**（`tools/_fix_core_b1.py`~`b9.py`）+ 逐字段规范化重写（283/424/268/274 为 AI 长解答，整段重写）。
- 验证：页面级渲染残留 56→0；逐段 KaTeX `throwOnError:true` 733 段 0 失败；JSON 合法 606 题不变；token 级内容一致性通过。
- 3 处数据残缺留注记「（原解答此处数据残缺，已省略）」：core-146 answer/idea 尾部、core-353 answer/idea 尾部。
- commit `06e6b9b` 已推送，远程 HEAD 一致。CACHE_VER 更新为 `mathcards-82d6e5e288`。
- 新增验证脚本：`tools/_verify_content.py`（bak token 级对比）、`tools/_todo_core_full.json`（清单完整字段导出）。
- 排查工具沉淀在 `C:/Users/cjx/.workbuddy/binaries/node/workspace/`：`_t_full.js`（全量体检）、`_t_locate_core.js`（残留定位）、`_t_kcheck.js`（逐段渲染自检）、`_t_cmd.js`（KaTeX 命令支持探测）。
- 遗留：exam 真题侧 6 个 bad（2021-19/2016-23/2014-21/2006-15/2003-13/2003-22）不在本清单。
## 【完成】全库渲染残留 215 → 2（commit 108063d 等，历经两天多轮）
- **成果**：残留 LaTeX 原文字段从 215 → **2**（99.8% 清零），KaTeX warn 136 → 127。
- 手段组合：12 批手工精确替换 + 批量规则 v3（3 轮）+ 子代理（exam 37 ✅ / core 56 ✅，QA 验证零内容丢失）
  + **渲染层增强**（4 份 mdBlock 支持「同行首尾 $$」如 `$$\begin{cases}...$$`）。
- **QA 教训**：子代理修复必须做「内容完整性」检查（git 对比去 $ 相似度 + 删除片段筛查）——
  exam 侧子代理曾误删 2016-16 评分参考与 2003-13 推导，已回滚恢复（内容优先于排版）。
- **残余 2 个顽固字段**（2003-13/22）：一行多块 $$ 挤行 + 中文叙述叠加，内容完整仅显示瑕疵，列遗留。
- **高频修复模式**（可复用）：块间缺开块补 `\n$$`；中文行内裸公式包 $；`\xlongequal`→`\overset`；
  `(**)` 星号被 mdInline 粗体误伤 → `(\text{*}\text{*})`；`$$$$`/孤立 `.$$` 清理；多块挤一行 → 拆行。
- 工具链：`_print_bad.py`、`_fix_hand*.py`、`_fix_batch.py`、`_t_full.js`（页面级全量体检）、
  `_qa_exam*.py`（内容完整性 QA）。CACHE_VER 9bb5950821。
